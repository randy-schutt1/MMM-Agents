# V10 — MASTERY REPORT

```text
STUDENT STATUS:  REVIEW REQUIRED
```

> **This is a SELF-ASSESSMENT and a SUBMISSION, not an authorization** (`D-016` / `I-001`).
> Only an independent reviewer's `PASS` makes V10 `COMPLETE` (`D-003`, `D-004`).

| Field | Value |
|---|---|
| Lesson | **V10** · `Bootcamp1 Wk3 040112 (96mins).swf` |
| SHA-256 | `a37ba371ca2d5c807553c7b9a827a91c479509dd5223b64eadf85995481a3de1` — re-verified before **and after** the frame-rate patch |
| Duration | 01:36:16 (5776.222 s audio; 11,553 frames ÷ 2.0 fps = 5776.5 s; manifest 5776 s) |
| Session date | **2012-04-01** — filename, **and stated in-recording** at `[00:21:25]` |
| Printed banner | `Market Makers Boot Camp` / `Week 3` (frame `00-00-33`) · **no topic title asserted** |
| **Speaker** | **THE COURSE AUTHOR, 100% OF RUNTIME** |
| Branch | `video/v10`, dedicated worktree (`D-038`). **No `I-009` collision.** |
| Gate | **Verified OPEN in `REVIEW_INDEX.md` before any V10 artifact was created.** V09 R1 returned `REVISE` 0 CRITICAL / 0 MAJOR / 6 MINOR → opens under `D-024` |

---

## ⭐ THE HEADLINE: THE FIVE-LESSON GUEST RUN ENDS

**V05, V06, V07, V08 and V09 each carried ZERO course-author runtime. V10 carries 100%.**

`COURSE_PROGRESS.md`'s V10 GATE carry-forward item (a) named a new week and a new session date as
*"a REASON TO EXPECT A CHANGE, not a reason to assume one"* and instructed that it be **tested**.
It was, on **five non-acoustic strands fixed before the answer was known**, with the cross-file
acoustic screen **not run** as V07's carry-forward prohibits:

1. The speaker claims **Steve's mailbox in the first person** — `[00:07:12]`, printed
   `steve@marketmakersforex.com` at frame `00-07-08`.
2. **All 13 `Steve` tokens are vocative or self-quoting** — he voices students addressing him and
   answers in the first person. In V04–V09 the guests referred to Steve in the **third person as an
   absent authority**; here the name only ever points at the microphone.
3. He **reads mail addressed to himself**, and the slide prints *"Hello Steve"*.
4. He claims **authorship of the method and the course** — `[00:12:45]`, `[01:28:11]`.
5. He **owns the homework loop end to end**.

**Negative check:** a scan for handover language returns **zero matches in 96 minutes**.

---

## THE TEN DIMENSIONS

### A. Recall — **SATISFIED**

The lesson's content is reproduced with timestamps in `V10_SOURCE_NOTES.md` (15 sections) and
verified by a **12/12 mechanical comprehension probe over 44 assertions**, answers committed at
`54b97f2` **before the scorer existed**.

**`V10_HOMEWORK.md` §4 argues against over-crediting the 12/12** — the same session wrote answers
and scorer, four questions were designed with their distractors in view, and the two hardest
answers are refusals. **The reviewer is pointed at `Q-011`, `C-016`/`C-017` and `BT_V10_0001` §1 as
the load-bearing evidence instead.**

### B. Recognition — **NOT SATISFIED. `D-030`-BLOCKED. Scored, not carved out. NO severity charge.**

**Sixth consecutive lesson.** The safety trade cannot be recognised on a chart because five of its
seven conditions rest on undefined terms (`A-076`, `A-077`, `A-007`, `A-004`, `consolidation`).

**V10 improves the position without unblocking it.** It supplies the **anchor** — `[01:14:06]`,
the week's extreme — which is fully measurable and was applied to real data in
`05_HOMEWORK/V10/scripts/h2_anchors.py`. **What it does not supply is the lock threshold**, and
without that the anchor is retrospective, so recognition remains impossible **prospectively**.

**This is recorded as a score, not a waiver.** `REVIEW_INDEX.md` open item **36** is escalated for
the **sixth** time.

### C. Discrimination — **SATISFIED**

The lesson supplies its own discriminations and they are captured:

- **A valid M that LOST, on timing** — `[00:23:08]`–`[00:23:34]`, *"the reason the red box didn't
  work out is because the timing baby"*. Correct pattern, wrong moment: the corpus's cleanest
  separation of **rule application** from **outcome**.
- **A NEGATIVE case in the lesson's own voice** — frame `01-07-52` / `[01:01:38]`: *"He does not
  make a visible stop hunt … because he doesn't exploit the Asian range."*
- **A no-go** — `[01:08:30]` *"this is a no-go, do not fall for that"*.
- **Take the W, not the M** — `[01:21:48]`, resolved by *"trading in line with the peak"*.
- **The DNC**, which is a discrimination rule in prohibition form.

**Beyond the lesson**, discrimination is demonstrated where it counts: `Q-011` separates a
**genuine V10 frame** from an **invented description** of it, and `A-078` separates a **settled
number** from an **unsettled reference point**.

### D. Sequence — **SATISFIED**

The nine printed rules are recorded **in the order the slides print them**
(`V10_SOURCE_NOTES.md` §6c), with the spoken marker beside each, and the two passes agree. The
lesson's own priority ordering — *"the pattern, the pattern, the pattern, timing, the levels"*
`[00:23:36]` — is quoted rather than reordered. Day numbering is captured including its
counter-intuitive origin: `[01:26:39]` *"Sunday is not day one."*

### E. Exceptions — **SATISFIED**

Every qualifier is carried, and two are carried **into the backtest** where dropping them would
have been convenient:

- the 600–1000 band's upper end belongs to **crosses** (`[00:14:26]`) — which puts GBP/USD nearer
  600 and makes the **favourable** reading the one on trial;
- *"unless he's shifting the zone"* (`[00:14:43]`) — recorded as **unoperationalisable**, so it
  cannot be invoked retroactively to rescue a failure (`E09`);
- *"never fails"* **withdrawn in the same breath** (`[01:31:26]`) — `D-009`;
- *"obvious ones are still valid"* (`[00:46:43]`), which **relaxes** the blue-box requirement the
  previous rule states;
- the **strict novice variant** (`[01:01:48]`, `[01:26:06]`) and its stated cost: 20 trades → 5–6.

### F. Homework — **SATISFIED (with one DEFERRED item, correctly classified)**

| Item | Disposition |
|---|---|
| H1 flashcards | **DONE** — five cards |
| **H2 mark 10 safety setups** | **ANCHORS DONE (10, cross-checked); SETUPS `DEFERRED`** |
| H3 post in the forum | **`NOT APPLICABLE`** (`D-018`) — a 2012 venue, no subject matter |

**The `D-019` classification is the part inviting audit, and it is argued rather than asserted**
(`V10_HOMEWORK.md`, disposition summary): H2 **has** subject matter — the lesson spends fifty
minutes on it — and is blocked **definitionally**, so it is `DEFERRED` and stays open. Marking it
`NOT APPLICABLE` would permanently discard performable research, which is the error `D-019` exists
to prevent.

**An independent cross-check was run on the H2 conclusion** (M1 raw vs M15 aggregated, two paths,
no shared code, 10/10 exact, gated by a non-zero exit) — **and its worth is stated honestly**: it
corroborates the aggregation, not the market.

### G. Manual Backtesting — **SATISFIED**

`PT-036` → `BT_V10_0001`. **Both claims CONTRADICTED AS STATED**, and `M2` additionally **fails its
own specificity test**.

| | Result |
|---|---|
| `M1` weekly range 600–1000 pips | **0 / 180 weeks = 0.00%.** Median 243.8; 600 sits at the **99.44th percentile** |
| `M2` Friday close 25–50 off both extremes | **7.30% (A) / 5.62% (B)** against the word *"always"* |
| `N2` matched uniform-close null | expected 7.12% vs observed 7.30% → **+0.19 pp.** On Arm B, observed is **below** the null |
| `N3` weekday specificity | **Friday ranks 4th of 5.** Thursday 13.41% vs Friday 7.30%; Friday − best other = **−6.10 / −8.91 pp** |
| `N4` shifted-boundary control | medians 243.8 vs 244.1 → **indistinguishable**; `M1` measures five days of travel, not week structure |
| Predictions | **6/6** on the genuine set, P7 flagged cheap and tallied separately |

**Three things this dimension should be credited for beyond the numbers:**

1. **The safety trade was NOT tested, and not testing it is reported as a finding** (`PT-036` §2,
   `BT_V10_0001` §1) with the hazard named: its anchor is retrospective, so a naive test would use
   the actual weekly extreme and commit **lookahead** (`E08`) while producing a flattering number.
2. **Open item 80's censoring bias was designed out and VERIFIED** — `censored = 0` on both arms,
   enforced as a hard assert that would have **voided** both measures.
3. **The 6/6 prediction tally is argued down in its own record** — P1/P2 are one finding, P3/P4/P5
   are one finding, only P6 was independent. Reporting 6/6 unqualified would inflate a calibration
   record.

**Also disclosed against interest:** the committed runner **crashed** on first execution and was
fixed before any number existed (`BT_V10_0001` §8), with the reasoning for why that is not a
`COMMON_PROTOCOL.md` §9 rule 7 violation.

### H. Provenance — **SATISFIED**

**408 marker citations across six V10 artifacts, ZERO orphans** (the one flagged is an explicitly
labelled V09 cross-reference). **134 quotations checked**; 127 resolve verbatim, and the 7 that do
not are **5 `PRINTED`-tagged slide texts, 1 project label, and 1 that is the documented ASR garble
at `[00:15:12]`** — i.e. all seven are self-consistent with their own tags.

Every note row carries a **basis tag** (`AUDIO` / `PRINTED` / `AUDIO+PRINTED`). **Every rule in
§6 is `AUDIO` or `AUDIO+PRINTED`; none rests on `PRINTED` alone**, so a reviewer can strike the
printed class and the lesson's contribution survives.

Frame references use the **burned-in player timecode**, not ordinals — open item 76's rule,
followed from the start rather than retrofitted. **32/32 burned timecodes were read from a
verification strip** and agree with the linear map to ≤ 2 s.

### I. Ambiguity — **SATISFIED**

Four new records, each with what is missing stated explicitly and each `DO NOT CODE`:

| | |
|---|---|
| **`A-076`** | `blue box` — the reference boundary of V10's own trigger, undefined, and `[00:46:43]` explicitly admits setups that violate it |
| **`A-077`** | **the lock** — V10's real-time substitute for a retrospective definition, with no threshold. **The most consequential gap in the lesson** |
| **`A-078`** | the anchor distance — **number RESOLVED** by the printed slide, **reference point NOT**. Deliberately split |
| **`A-079`** | `blue tracer` — named 13×, used as a reference level, never defined; *"separates the day"* reads vertical while *"pips off of"* requires horizontal |

Plus **`A-010` NARROWED** on Tier 1 evidence alone (`SOURCING_HIERARCHY.md` §3.2 case A, **not
blended** with `MMM-NOTES`) — the first operational definition of `peak formation` in ten lessons.

**No machine constant was proposed for anything the lesson left subjective.** Three candidates are
recorded and fenced `INFERRED MACHINE CANDIDATE / NOT A COURSE RULE` (`V10_INTERPRETATION.md` Q9).

### J. Contradictions — **SATISFIED**

| | |
|---|---|
| **`C-016`** | the directional lock is *"one-day"* and *"good for three days"* **two seconds apart**, with *"at least two days"* twice more. Matters because the safety trade **has no stop** — its only non-target exit is time |
| **`C-017`** | the anchor distance is measured from the **LOW/HOW anchor in print** and from the **blue tracer in speech** (4 of 6 spoken instances). **The corpus's first printed-vs-spoken conflict** |

**A tidy reconciliation was available for both and BOTH ARE REFUSED IN WRITING**, per the `C-010`
precedent. For `C-017` the refusal is argued rather than asserted: `D-008` would settle it by source
class, and **is not applied**, because `D-008` ranks *capture reliability* — it was written to stop
an agent's reading outranking the course, not to adjudicate between two things one speaker said in
one hour.

**No `C-xxx` is opened against `MMM-NOTES`:** V10 contradicts nothing in the Tier 2 document. It is
silent where the document speaks and vice versa — recorded as a negative result.

---

## `SOURCING_HIERARCHY.md` §3.1 RECONCILIATION — RUN

| Term | Result |
|---|---|
| **peak formation high/low** | **Tier 1 SPOKE.** `A-010` narrows, case A, **course evidence alone** |
| `the level` (`A-004`) | **STAYS OPEN.** And `V10_INTERPRETATION.md` Q7 records that `[01:16:36]` **routes around it** — a student says they cannot identify level three and is given a workaround, not a definition |
| EMA nicknames (`A-020`, `C-010`) | **No new Tier 1 statement.** V10 uses `blueberry` and `Mayo` at `[01:25:28]` with **no period attached**; `800` occurs **0** times. **`C-010` not advanced, not closed** |
| ADR lookback (`A-038`, `C-011`) | **No change.** Fourth consecutive lesson to use ADR without defining its window |
| TDI (`A-039`) | **EXTENDED, not closed** — displayed in three frames, and **explicitly deferred to V11** at `[01:13:03]` |

---

## QUARANTINE — `Q-011`

`NOTES.md` and `VISUAL_INDEX.md` **fabricated** (10 of 21 audited, 10/10). Two results stronger than
prior rounds:

- **`RULES.md` is V01's file with SIX identifier strings swapped** — shown by `diff`, not by
  normalised hash. Every rule, timestamp and parameter is byte-identical to the copy `D-017`
  already disproved. **Six cited markers, six misses, zero partial hits.**
- **`NOTES.md`'s headline framing is CONTRADICTED IN TERMS** by the lesson at `[01:13:47]`, not
  merely unsupported. First of its kind in the register.
- **Second confirmed instance of `Q-010`'s sixth failure mode** — the one real image is a genuine
  V10 frame (the **title card**) carrying an entirely invented description.

**Two self-corrections are recorded rather than deleted:** an early `EMA` count of 24 that was
`email` substring matches (true count **2**), and this session's stricter hash normaliser calling
`RULES.md` unique where `diff` calls it V01's — **`Q-010`'s instrument was the better one.**

---

## SELF-ASSESSMENT — WHERE THIS SUBMISSION IS WEAKEST

`MASTERY_STANDARD.md` § *Self-assessment honesty*. Four items, strongest objection first.

1. **`A-010`'s narrowing rests on a single relative clause in a digression, stated once at minute
   74 of 96, and not printed in that form.** `V10_INTERPRETATION.md` Q8 asks the falsification
   question against this session and concludes the record should **NARROW, not CLOSE**. **If a
   reviewer reads `[01:14:06]` as an aside, `A-010` should stay where it was — and nothing else in
   this submission changes**, because `PT-036` measures weekly ranges from OHLC and never uses the
   term.
2. **The evidence-order rule was met in substance but not perfectly.** The full transcript was read
   and this file's understanding formed before any frame was curated — **but two categories of
   image were opened earlier**, both required by the protocol itself: the quarantined image for
   `Q-011`, and the `GOTCHA 4` sanity frame. **Neither contributed a rule**, and the basis tags let
   a reviewer verify that. Disclosed as a deviation, not defended as compliance.
3. **The 12/12 comprehension score is weak evidence** and `V10_HOMEWORK.md` §4 says so in three
   specific ways. **It should not carry much weight.**
4. **`PT-036` tests V10's context claims, not V10's trade.** A reviewer may reasonably hold that a
   lesson whose signature trade is untestable has not been *empirically* engaged. **The counter is
   `D-030`, and the counter-counter is that `D-030` produces exactly this accumulating debt** —
   which is the correct behaviour, and is now six lessons deep.

---

## ESCALATIONS

1. **Dimension B `D-030`-blocked for the SIXTH consecutive lesson.** `REVIEW_INDEX.md` open item
   **36** needs an **owner** ruling; a student session cannot supply one.
2. **`SWF_CAPTURE_RECIPE.md` §10 states the frame-rate patch as `3.0 → 30.0` fps, from V01/V02.
   V10 declares 2.0.** Applied literally it sweeps at **15×, not 10×**, and **fails silently** —
   well-formed, correctly-timecoded frames at 7.5-second spacing, under-sampling the detector by a
   third. **POLICY ledger under `D-038a` — escalated, NOT patched from this branch.** The fix is one
   line, and `GOTCHA 5`'s probe already parses the adjacent header field.
3. **`A-077` (the lock) is the highest-value gap V10 leaves.** It is the *only* thing standing
   between V10's now-defined anchor and a prospectively identifiable setup. Any lesson attaching a
   distance or a duration to *"price has moved away and confirmed"* discharges dimension B's block,
   H2's deferred half, and `PT-038`.
4. **`A-004` is still the project's largest single blocker after ten lessons** — **and V10 shows the
   course routing around it rather than resolving it** (`[01:16:36]`). Worth an owner's attention:
   this may be a term the course never defines.
5. **`C-017` is the corpus's first printed-vs-spoken conflict** and the project has no standing rule
   for that class. `D-008` is the nearest instrument and this session declined to apply it, with
   reasons. **An owner or reviewer ruling would settle a question that will recur.**
6. **Two `PT` successors specified and not run** — `PT-037` (the path-length reading of `M1`, which
   **must be pre-registered before measuring**, since running it after seeing a failure is how a
   contradicted claim gets rescued) and `PT-038` (the safety trade, blocked by `D-030`).

---

## VALIDATOR

`scripts/validate_project.py` — see `LOG.md` for the run at session close.

---

## DECISION

```text
STUDENT STATUS: REVIEW REQUIRED

A submission, not an authorization (D-016 / I-001). This session did not
review itself (D-003) and did not merge to integration -- merge-back is a
separate, single-threaded act under D-038.

NEXT: independent review R1 of V10.
```
