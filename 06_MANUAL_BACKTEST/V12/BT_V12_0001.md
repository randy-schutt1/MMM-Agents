# BT_V12_0001 — `PT-040` observation report

```text
TEST:        PT-040 -- Does A-084's smoothing ambiguity change the RSI thresholds V11 states?
PRE-REG:     06_MANUAL_BACKTEST/PRE_REGISTERED/PT-040_does_the_A084_smoothing_ambiguity_
             change_the_rsi_thresholds.md
             committed 83110f1, 2026-08-13T22:00:37-04:00, BEFORE run_pt040.py existed.
RUNNER:      06_MANUAL_BACKTEST/scripts/run_pt040.py
OUTPUT:      06_MANUAL_BACKTEST/V12/data/pt040_output.txt  (committed verbatim, D-027)
SESSION:     V12 student, branch video/v12, isolated worktree (D-038)
DATE:        2026-08-13
```

---

## 1. CLASSIFICATION — `BACKTEST_EVIDENCE_STANDARD.md`

| Observation | Classification | Why |
|---|---|---|
| **`O2` — side disagreement (the primary)** | ⭐ **`EVIDENTIAL`** | Rule and period **pre-registered**; the decision boundaries (`2 pp` / `5 pp`) were fixed before the run and are not adjustable; `n = 24,730` bars; a baseline is built in (`k = 1` is the identity control, and `R` itself is the comparator); **two independent robustness cells (`N2` window, `N3` formula) and both `D-031` arms reported** |
| `O1` — marginal occupancy | **`DESCRIPTIVE`** | A measurement of the series. It has no baseline and tests no claim |
| `O3` — crossing-count ratio | **`DESCRIPTIVE`** | Reported as a sanity check on the smoothing, with a pre-registered direction (`< 1.0`) but no decision attached |
| `O4` — bull/bear range occupancy | **`DESCRIPTIVE`** | **Pre-registered as descriptive in `PT-040` §4.** V11's range claims are conditional on an undefined trend (`A-070`, `A-004`), so nothing here tests them |

### 1a. Integrity, deviations and disclosures

| # | Item |
|---|---|
| 1 | **The pre-registration precedes the runner by commit, verifiably.** `83110f1` (2026-08-13T22:00:37-04:00) contains the design and **no `run_pt040.py`** — confirmed by `ls` at that commit before it was made. `D-026` satisfied by ordering, not by assertion |
| 2 | **No deviation from the pre-registration.** Every threshold, smoothing length, window, arm, observation and decision boundary in the run is the one `PT-040` fixed. `COMMON_PROTOCOL.md` §9 rule 7 was not invoked |
| 3 | **`stage → validate → commit`, in that order.** `REVIEW_INDEX.md` item 108 recorded that V11 ran the validator before staging and shipped one commit in a failing state. **This session staged first and validated second on every commit**, including this one |
| 4 | **⚠️ The header's `seed : 20260812` is `mmm_lib`'s batch constant, not this test's.** `REVIEW_INDEX.md` item 113 raised this as a library defect on `PT-039`. **It is harmless here and `PT-040` §7 said so in advance: this test uses NO randomisation** — no bootstrap, no permutation, no shuffle. Every figure is a deterministic count. **`SEED` is declared in `run_pt040.py:26` and never read.** The library fix item 113 recommends is still owed on the integration branch |
| 5 | **No holdout contact.** `mmm_lib.assert_development` is asserted on every slice via `L.window()`; the run exits non-zero on breach and did not |
| 6 | **QA gate is a precondition and PASSED** — `GATE: PASS — C1-C4 clean`, printed in the output header with the four-file SHA-256 manifest |
| 7 | **Nothing was read from a rendering.** Closes are parsed from the checksummed CSV corpus and aggregated by `mmm_lib.resample`. No chart was opened for this test |

---

## 2. `N1` — SAMPLE, REPORTED BEFORE ANY RATE

| Cell | Bars in window | Valid after warm-up + smoothing | Discarded | Span |
|---|---|---|---|---|
| `W-A` / Arm A **(PRIMARY)** | 24,755 | **24,730** | 25 | 2015-01-04T17:00 → 2015-12-31T16:45 |
| `W-A` / Arm B | 24,755 | 24,730 | 25 | same series, relabelled |
| `W-B` / Arm A | — | see output | — | 2014-01-05 → 2015-12-31 |
| `W-B` / Arm B | — | see output | — | — |

**25 bars discarded**: 21 to the RSI warm-up and 4 more to the `k = 5` smoothing window.
**0.1% of the sample.**

---

## 3. ⭐ THE RESULT — **`MATERIAL`**, AND IT IS THE OPPOSITE OF WHAT THIS SESSION WANTED

### `O2` — side disagreement vs `R = RSI(21)`, percentage points, `W-A` / Arm A

| `k` | `t=20` | `t=40` | **`t=50`** | `t=60` | `t=80` |
|---|---|---|---|---|---|
| **2** | 0.08 | 2.78 | **5.16** | 2.45 | 0.04 |
| **3** | 0.12 | 4.15 | **7.48** | 3.64 | 0.08 |
| **5** | 0.18 | 5.78 | **10.48** | 5.16 | 0.09 |

```text
M = 10.481 pp,  attained at k = 5, t = 50
PRE-REGISTERED BOUNDARIES:  <= 2.0 IMMATERIAL  |  2.0-5.0 INCONCLUSIVE  |  > 5.0 MATERIAL

VERDICT: MATERIAL
```

### What it means — the pre-registered consequence, quoted from `PT-040` §5

> *"The choice of smoothing changes the answer. **V11's RSI threshold claims STAY
> BLOCKED** pending a course statement of the smoothing length, and `A-084` is promoted
> from a definitional note to an active blocker."*

**`A-084` is therefore PROMOTED TO AN ACTIVE BLOCKER**, and the record is updated to say so.

> ### ⭐ THIS IS THE PRE-REGISTRATION DISCIPLINE PAYING FOR ITSELF, AND IT SHOULD BE READ THAT WAY
>
> **This session closed `A-080` two hours earlier and wanted the unblock.** The whole
> value of V12's headline finding — the course finally stating a parameter — is that it
> lets a blocked half of V11 become testable. **The session that had every incentive to
> find `A-084` immaterial wrote the bar down first, at 2 pp and 5 pp, before computing a
> single RSI value, and then measured 10.48 pp.**
>
> Had the boundary been chosen after seeing the numbers, **`M = 10.48` sits close enough
> to a defensible-sounding "10 pp is the real threshold" that the temptation would have
> been live.** It was foreclosed by `D-029` and by a file committed before the runner
> existed.
>
> **The honest summary of V12's backtest is: the lesson supplied the number, and the
> number did not unblock what it was supposed to unblock.**

### And it does not depend on the choice of `k` in the way one might hope

**Even at `k = 2`** — the shortest non-trivial smoothing, and the shipped TDI's own
default — **the 50-line disagreement is `5.16 pp`, already past the `MATERIAL` boundary.**
There is no smoothing length in the swept family at which the ambiguity is safe.

**The disagreement is concentrated exactly where it hurts most.** `t = 50` is the
**bias baseline** — V11's single most-used RSI rule (*"above the 50 the bias is up"*),
restated by V12 at `[00:13:32]` and `[00:13:44]`. It is the threshold the series sits
nearest to and therefore crosses most (2,459 crossings on `R` in one year, `O3`), and it
is the one the smoothing disturbs most.

---

## 4. ROBUSTNESS — every pre-registered secondary check, and all four hold

| Check | Pre-registered expectation | Measured | |
|---|---|---|---|
| **Arm A vs Arm B** | agree within `0.5 pp` on every `O2` cell | **`0.000 pp`** — *exactly* zero | ✅ |
| **`O1(k=1, t=50)`** | in `[45%, 55%]` | **`49.11%`** | ✅ |
| **`O3(k,t) < 1.0` for `k ≥ 2`** | a smoothed series crosses less | all ratios `0.20`–`0.74` | ✅ |
| **`N2` — `W-B` / Arm A** | same verdict band as `W-A` | `M = 10.661 pp` → **`MATERIAL`**, same band | ✅ |
| **`N3` — simple-average RSI** | reported as a line | `M = 12.147 pp` → **`MATERIAL`**, same band, slightly larger | ✅ |

> ### The `0.000 pp` arm agreement is worth one sentence, because it is a CHECK THAT COULD HAVE FAILED
>
> `PT-040` §3.4 pre-registered that the `D-031` arms **must** agree almost exactly here,
> because this test's unit of analysis is **the bar**, not the session day, so
> `REVIEW_INDEX.md` item 101's Arm-B defect cannot bite — **a clock relabel changes which
> bar is called what, not the close series.** It also pre-registered that a material
> disagreement would be **a tooling bug, not a finding**.
>
> **The measured difference is exactly `0.000 pp`**, which is the strongest possible form
> of that expectation being met, and it is **positive evidence that `mmm_lib.shift_to_arm`
> does what it claims** — a thing item 101 put in question for a different reason.
> **`PT-040` is the first test since item 101 for which Arm B is a clean robustness cell**,
> and it comes back clean.

---

## 5. `DESCRIPTIVE` OBSERVATIONS — reported, and tied to no verdict

### `O1` — marginal occupancy, `W-A` / Arm A, % of bars with `S_k ≥ t`

| `k` | `t=20` | `t=40` | `t=50` | `t=60` | `t=80` |
|---|---|---|---|---|---|
| **1** (`= RSI(21)`) | 99.74 | 85.14 | 49.11 | 13.27 | **0.11** |
| 2 | 99.79 | 85.47 | 49.10 | 12.85 | 0.08 |
| 3 | 99.79 | 85.78 | 49.18 | 12.63 | 0.06 |
| 5 | 99.84 | 86.20 | 49.26 | 12.29 | 0.04 |

> ### ⭐ AN UNANTICIPATED READING, AND BOTH DIRECTIONS ARE GIVEN
>
> **`RSI(21)` on GBP/USD M15 reaches `80` on `0.11%` of bars and falls below `20` on
> `0.26%`.** V11's printed *"Overbought/Oversold 80/20"* therefore describes an event that
> occurs roughly **once every 900 bars** — about **nine trading days** at 96 bars a day.
>
> **This cuts two ways and the report declines to choose:**
>
> - **FOR the reading that `21` is right:** V12 `[00:25:38]` says reaching *"the extreme
>   extreme extreme on TDI"* requires *"**dropping for a few days — two and a half days,
>   three days, four days**."* **A once-in-nine-days event is the right order of magnitude
>   for that sentence.** The rarity is what the instructor describes.
> - **AGAINST:** an oscillator whose stated overbought line is touched on one bar in nine
>   hundred is an odd thing to print as a *"parameter"* on a teaching slide, and a shorter
>   period would reach it far more often.
>
> **Neither reading is adopted. `O1` is `DESCRIPTIVE` and it tests nothing** — the claims
> it brushes against are trend-conditional and `A-070`/`A-004` block them. It is recorded
> because it is the first quantitative fact the project has ever had about the course's own
> RSI period, and a later session will want it.

### `O4` — range occupancy (`DESCRIPTIVE`, pre-registered as such)

`RSI(21)` sits inside the printed bull range `[40, 80]` on **85.03%** of bars and inside
the bear range `[20, 60]` on **86.47%** — and inside **both at once on 71.87%**, because
the two ranges overlap on `[40, 60]`.

**This tests nothing and must not be read as testing anything.** The claim is that the
range *switches* with the trend, and the trend is undefined. **It is reported only because
a reader looking at V11's most-quoted RSI content will want the marginals, and because the
71.87% overlap makes concrete why the switch cannot be detected without a trend
definition:** on seven bars in ten, both ranges are satisfied simultaneously.

---

## 6. WHAT THIS DOES NOT SHOW — restated from `PT-040` §2 because it is quotable

1. **It does not test whether V11's RSI threshold claims are true.** They are conditional
   on an undefined trend.
2. **It does not test the TDI.** Three of four parameters are unstated; no TDI was built.
3. **It adopts no smoothing length.** `k ∈ {1,2,3,5}` is a sweep over the ambiguity.
   **`k = 5` attaining `M` is not a claim that the line is smoothed by 5.**
4. **It does not test V12's `85%` claim.** Blocked on `A-011` and `A-076`.
5. **It does not re-verify `A-080`.** It consumes that closure.

---

## 7. RECORD EFFECTS

| Record | Effect |
|---|---|
| **`A-084`** | ⭐ **PROMOTED TO AN ACTIVE BLOCKER**, on this test's pre-registered decision rule. Status text updated in `AUTOMATION_AMBIGUITIES.md` |
| **`A-080`** | **UNCHANGED — still `RESOLVED BY COURSE`.** This test consumes it and does not question it. The period is known; the *series it is the period of* is the open question |
| **V11's RSI claims** | **STAY BLOCKED.** `A-080`'s closure is necessary and **not sufficient** |
| **`A-039`** | **UNCHANGED.** This result strengthens the reasoning in `V12_INTERPRETATION.md` Q1 for narrowing rather than closing |
| **`D-031`** | **No change proposed.** But this run is a **positive** data point on Arm B's tooling in a bar-unit test, recorded beside item 101's negative one in a day-unit test |

## 8. WHAT WOULD CHANGE THIS RESULT

- **A course statement of the smoothing length**, which would replace the sweep with a
  single series and make V11's claims directly testable.
- **A statement that the plotted line IS the RSI**, which would set `k = 1` and unblock
  everything immediately — `O2(1, ·) = 0` by construction.
- **A demonstration that the plotted line is smoothed by something outside the swept
  family** (EMA, Wilder), which would make `M` the wrong quantity — named as limitation 1
  in `PT-040` §6.

**V13 (`Wk5 041512 Part1`) is the next place any of the three could appear**, and it is
the cheapest remaining unblock in the project.
