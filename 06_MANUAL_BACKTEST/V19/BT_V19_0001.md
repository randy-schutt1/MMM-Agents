# BT_V19_0001 — PT-047: V19's second-leg time cap and the 25–50 pip extension

**Test:** `06_MANUAL_BACKTEST/PRE_REGISTERED/PT-047_the_second_leg_time_cap_and_the_extension.md`
**Pre-registration committed:** `b34d1a1`, **before `run_pt047.py` existed and before any bar was
read.**
**Runner:** `06_MANUAL_BACKTEST/scripts/run_pt047.py`
**Raw output:** `data/pt047_results.json`, `data/pt047_output.txt`
**Instrument / corpus:** GBP/USD, HistData M1 → M15 via `mmm_lib`, DEVELOPMENT scope only
(`D-035`; the 2016-07-01 → 2016-12-31 holdout is sealed and not on disk).
**Seed:** `20260812`. **Permutation iterations:** 10,000.

---

## §0a — EVIDENCE CLASSIFICATION (`BACKTEST_EVIDENCE_STANDARD.md`)

**This observation is classified `EVIDENTIAL`.**

It is `EVIDENTIAL` rather than `DESCRIPTIVE` because it was **pre-registered before any bar was
read** (`b34d1a1`, committed before `run_pt047.py` existed), it carries a **pre-declared baseline**
(`N1` label permutation, `D-026`), a **pre-declared fragility guard** (`N3`) and a **four-branch
decision rule fixed in advance**, and every cell has `n ≥ 31` per group.

> ### ⚠️⚠️ AMENDED 2026-08-15 — `REVIEW_INDEX.md` item **302**, `V19_REVIEW_R1.md` `M1`
>
> **AS ORIGINALLY FILED THIS CLASSIFICATION WAS NOT EARNED.** `BACKTEST_EVIDENCE_STANDARD.md` §5
> defines `EVIDENTIAL` as *"Pre-registered rule and period, baseline run, n ≥ 30, **interval
> reported**"*, and §4.2 requires an interval on **every** rate. **This report as first committed
> contained no interval of any kind** — not on `Δ`, not on the medians, not on `O2`/`O3`. V19 R1
> charged that as a **`MAJOR`** (`E24`) and it was correct to.
>
> ⭐ **The intervals are now supplied in §2a and in §2's table, and the `EVIDENTIAL` classification
> stands from this amendment forward.** ⛔ **The verdict, the decision rule and `PT-047` are
> UNCHANGED** — see §2a's closing paragraph for why changing them would have been the wrong fix.

⚠️ **`EVIDENTIAL` is not `TRADEABLE`.** No spread, slippage, commission, execution or position
sizing is modelled; `D-006` defers all of that to Phase 8. **And §4's disjoint-window re-measurement
is explicitly `DESCRIPTIVE`, not `EVIDENTIAL`** — it was run after seeing the pre-registered result
and is labelled post-hoc throughout.

---

## §0 — VERDICT

> ## ⭐ **CONFIRMED**
>
> Primary cell (arm **A**, window **`W-A`**, **30-minute** classifier):
> **Δ = +25.75 pips**, permutation **p = 0.0007**, **median MFE(`HELD_ABOVE`) = 43.2 pips** —
> inside the lesson's own `[25, 50]` band. **`N3` did not fire.**
>
> **All eight cells agree in direction. All eight have p ≤ 0.004.**

> ### ⚠️⚠️ AND READ THIS BEFORE CITING THE MAGNITUDE — ADDED 2026-08-15 (item **302**)
>
> **The `CONFIRMED` verdict has two magnitude legs, and NEITHER IS RESOLVABLE AT 95 % ON THIS
> SAMPLE:**
>
> * **`Δ = +25.75` pips**, bootstrap 95 % ≈ **`[7.9, 38.9]`** — **the lower bound is BELOW the
>   pre-registered `Δ ≥ 10` materiality floor.**
> * **median `O1`(`HELD_ABOVE`) = 43.20 pips**, exact distribution-free CI **`[30.40, 55.40]`** —
>   **the upper bound is ABOVE 50, the top of the band the verdict requires it to lie inside.**
>
> ⭐ **What IS established at 95 % is the DIRECTION and the NON-RANDOMNESS**, and those are strong:
> permutation `p` = 0.0007 (0.0004 on the reviewer's independent seed), Mann-Whitney two-sided
> `p` = 7.0 × 10⁻⁵, all eight cells positive, and the effect survives a disjoint-window
> re-measurement and every robustness probe run against it.
>
> ⛔ **CITE THIS AS: "closing back below within 30 minutes separates two excursion populations by a
> material, non-random margin, with the held-above median near the middle of the lesson's stated
> band."** ⛔ **DO NOT CITE IT AS: "the extension is 25–50 pips."** Full working in §2a.

⚠️ **READ §4 BEFORE CITING THIS.** A structural objection that the pre-registration **failed to
declare** is raised and tested there, post-hoc. **The result survives it** — but the omission is
this test's own defect and is reported as one.

---

## §1 — WHAT WAS TESTED

V19's central rule, spoken for the M at `[00:22:43]`, mirrored for the W at `[00:46:38]`, confirmed
verbatim on an independent ASR engine, and **written into the deck on camera** at `23:25` / `50:34`:

> *"The second leg rise can be slightly above the first **but must close back below** — **within 30
> minutes**."* … *"If he stays above the previous high … he is gonna extend that high to the next
> level strike zone **25 to 50** higher."*

**Operationalised** (`PT-047` §3, fixed before the run): within a session day's post-box bars, the
**first** bar whose high exceeds a running day-high that is **8–24 bars old** is an *exceed event*;
the level `L` is that prior high. The event is **`CLOSED_BACK`** if either of the next two bars
closes below `L`, else **`HELD_ABOVE`**. `O1` is the maximum excursion above `L` over the following
**16 bars (4 h)**.

**No entry, exit, stop, target, position size or P&L is computed anywhere. This is not a strategy
backtest and must not be cited as one** (`PT-047` §1a).

---

## §2 — RESULTS, ALL EIGHT CELLS

| arm | window | cap | n | `HELD` | `BACK` | med `O1` `HELD` | med `O1` `BACK` | **Δ pips** | perm `p` |
|---|---|---|---|---|---|---|---|---|---|
| **A** | **`W-A`** | **30m** | **99** | **39** | **60** | **43.20** | **17.45** | **+25.75** | **0.0007** |
| A | `W-A` | 45m | 99 | 34 | 65 | 45.20 | 18.30 | +26.90 | 0.0004 |
| A | `W-B` | 30m | 206 | 69 | 137 | 35.50 | 16.30 | +19.20 | 0.0001 |
| A | `W-B` | 45m | 206 | 60 | 146 | 36.05 | 16.85 | +19.20 | 0.0001 |
| B | `W-A` | 30m | 98 | 36 | 62 | 44.20 | 23.85 | +20.35 | 0.0040 |
| B | `W-A` | 45m | 98 | 31 | 67 | 48.40 | 23.30 | +25.10 | 0.0007 |
| B | `W-B` | 30m | 207 | 73 | 134 | 34.40 | 17.75 | +16.65 | 0.0001 |
| B | `W-B` | 45m | 207 | 61 | 146 | 38.30 | 17.75 | +20.55 | 0.0001 |

**Primary cell inclusion accounting:** 256 session days included; **2 excluded** by `C-6`
completeness, before any event was identified. 99 of 256 days produced an exceed event under the
§3 definition — **the event is not common, and that is stated rather than buried.**

### The secondary outcomes, primary cell

**⚠️ Wilson 95 % intervals added 2026-08-15 (item 302). No percentage in this file may be read
without them.**

| ID | `HELD_ABOVE` | **Wilson 95 %** | `CLOSED_BACK` | **Wilson 95 %** |
|---|---|---|---|---|
| `O2` — reached ≥ 25 pips above `L` | **76.9 %** (30/39) | **`[61.7, 87.4]`** | 45.0 % (27/60) | **`[33.1, 57.5]`** |
| `O3` — landed **inside** `[25, 50]` | 33.3 % (13/39) | **`[20.6, 49.0]`** | — | — |
| `O3` — inside `[25, 55]` (`PT-047` §2d) | 43.6 % (17/39) | **`[29.3, 59.0]`** | — | — |
| `O4` — median MAE **below** `L` | **9.2 pips** | — | **31.85 pips** | — |

⭐ **`O2`'s two intervals do NOT overlap** (`[61.7, 87.4]` vs `[33.1, 57.5]`) — that separation is
real at 95 %. ⚠️ **`O3`'s interval is 28 percentage points wide on `n` = 39**, which is the honest
width of a one-in-three claim at this sample size, and is why the `O3` reading below is offered as
a reading rather than as a result.

⭐ **`O4` is the strongest single number in this test and it was not the decision criterion.** The
pre-registration measured the roll-off but declined to score it, because V19 gives no target for it.
**It separates the groups more cleanly than `O1` does, and in the direction the lesson predicts:**
events that closed back below the level subsequently traded **31.85 pips below it** at the median,
against **9.2 pips** for those that held. **The lesson's *"price appears to roll off of the level"*
is visible in the data it was not scored on.**

⚠ **`O3` is the weakest number and it is reported at face value.** Only **a third** of `HELD_ABOVE`
events land *inside* the `[25, 50]` band. **That is not a failure of the rule — most of the misses
are ABOVE 50, not below 25** (`O2` = 76.9 % reach 25+). **The lesson's band describes where the
*next level* sits, not a ceiling**, and this test cannot distinguish "extended to the next strike
zone and stopped" from "extended past it". **The `[25, 50]` band is therefore evidenced as a floor,
not as an interval**, and `PT-047` §5's use of it as an interval is the more demanding test — which
the median passed.

---

## §2a — ⚠️⚠️ THE INTERVALS — ADDED 2026-08-15 AT OWNER DIRECTION, DISCHARGING `REVIEW_INDEX.md` ITEM **302**

**This section did not exist when the verdict was filed. That is the defect it repairs.**
`BACKTEST_EVIDENCE_STANDARD.md` §4.2 requires an interval on every rate and §5 makes one a
condition of the `EVIDENTIAL` class this report claims. **V19 R1 charged the omission as `MAJOR`
(`E24`, item 302) and the charge was correct.**

### The primary cell, `A｜W-A｜30m`, `n_HELD` = 39, `n_BACK` = 60

| Quantity | Point | **Interval** | Method | Against what |
|---|---|---|---|---|
| **`Δ` — the decision statistic** | **+25.75 pips** | **≈ `[7.9, 38.9]`** | bootstrap 95 %, 20,000 resamples | ⚠️⚠️ **the pre-registered materiality floor is `Δ ≥ 10` — THE LOWER BOUND IS BELOW IT** |
| **median `O1`(`HELD_ABOVE`)** | **43.20 pips** | **`[30.40, 55.40]`** | ⭐ **exact distribution-free**, order statistics | ⚠️⚠️ **the verdict requires this INSIDE `[25, 50]` — THE UPPER BOUND IS ABOVE 50** |
| median `O1`(`CLOSED_BACK`) | 17.45 pips | `[9.90, 33.90]` | ⭐ exact distribution-free | — |
| Hodges-Lehmann shift | **+22.20 pips** | — | deterministic, no resampling | ⚠️ **3.55 pips BELOW the difference-of-medians**; the pre-registered statistic is the more favourable of the two |

⭐ **The two median intervals are EXACT and deterministic** — they are order-statistic intervals and
depend on no seed. Their achieved coverage is **97.6 %** (`HELD`) and **97.3 %** (`BACK`), not
exactly 95 %, because the attainable levels of a distribution-free median interval are discrete;
**they are therefore CONSERVATIVE, i.e. slightly wider than a true 95 % interval would be, and are
reported at their real coverage rather than relabelled.**

⚠️ **The `Δ` interval is a bootstrap and its endpoints carry Monte Carlo error of roughly ±0.3
pips.** Two seeds were run: `[7.90, 38.90]` and `[8.20, 38.90]`. ⭐ **The finding does not depend on
the seed — the lower bound sits below the floor of 10 on every seed tried**, and it is reported to
one decimal rather than two so the precision is not overstated.

### ⛔ WHAT THIS DOES **NOT** DO — AND WHY THE VERDICT IS UNCHANGED

**The `CONFIRMED` verdict stands, and re-opening it here would have been the WRONG fix.**

`PT-047` §5 fixed a four-branch decision rule **on point estimates, before any bar was read**. Every
one of its conditions is met on the point estimates and always was: `Δ` = 25.75 ≥ 10, `p` = 0.0007
< 0.05, the median lies inside `[25, 50]`, and `N3` did not fire. ⚠️⚠️ **Re-scoring a
pre-registered rule against a criterion invented after the numbers were seen is exactly the
failure `REVIEW_PROTOCOL.md` §17.5 names — performance chasing — and it does not become acceptable
merely because it runs in the conservative direction.** A pre-registration that may be tightened
after the fact is not a pre-registration.

⭐ **So the fix is ADDITIVE: the verdict keeps the meaning its own rule gave it, and the intervals
say how precisely that meaning is known.** Concretely:

* ✅ **ESTABLISHED at 95 %:** the time-cap clause separates two excursion populations, **in the
  direction the lesson predicts**, by a **material and non-random** margin. Permutation
  `p` = 0.0007, reviewer's independent seed `p` = 0.0004, **Mann-Whitney two-sided
  `p` = 7.0 × 10⁻⁵**, all eight cells positive, `N3` clear, and the effect **grows** under a
  disjoint-window re-measurement (§4).
* ⛔ **NOT ESTABLISHED at 95 %:** that the magnitude is **`25–50` pips** specifically, or that `Δ`
  clears the **10-pip** materiality floor. **Both are point-estimate findings whose intervals
  include failure.**

⚠️ **Any downstream artifact — `12_MASTER_SPEC`, `13_MACHINE_SPEC`, a Pine script, a `PT-` file
citing this one — must carry the second bullet.** `REVIEW_PROTOCOL.md` §18 asks whether real-money
execution should be allowed to depend on an interpretation; **the honest answer for the magnitude
leg of this result is NOT YET, and the number that would change it is a larger `n`, not a
re-reading.**

### Reproduction

```bash
cd 06_MANUAL_BACKTEST/scripts
python3 rev_pt047_independent.py     # the reviewer's independent re-implementation
```

Raw output: `06_MANUAL_BACKTEST/V19/data/rev_pt047_intervals.txt` (bootstrap, both seeds, and the
Wilson figures) and `rev_pt047_output.txt`. **`run_pt047.py` was re-run while writing this section
and `pt047_results.json` came back byte-identical to the committed file** — no number in §0 or §2
moved.

---

## §3 — THE `N3` FRAGILITY GUARD — **DID NOT FIRE**

Every condition was fixed in `PT-047` §4 before any number existed:

| Guard | Result |
|---|---|
| 30-min and 45-min arms disagree in direction | ✅ no — all eight cells positive |
| arms **A** and **B** disagree in direction | ✅ no |
| `W-A` and `W-B` disagree in direction | ✅ no |
| either group `n < 30` on any cell | ✅ no — the smallest is `n = 31` (B / `W-A` / 45m) |
| leave-one-out flips the primary verdict | ✅ no — Δ moves **25.75 → 25.55** |

⚠ **The `n < 30` guard came within one event of firing.** `B|W-A|45m` has `n_held = 31`. **Had the
robustness arm been 60 minutes rather than 45, this guard would very likely have fired and the
verdict would have been `FRAGILE`.** Recorded because the guard's margin is part of the result.

---

## §4 — ⚠⚠ THE OBJECTION THE PRE-REGISTRATION FAILED TO DECLARE — RAISED, TESTED POST-HOC, AND SURVIVED

**This is the most important section of this report and it is a self-reported defect.**

`PT-047` §2 declared five weaknesses in advance. **It missed a sixth, and it is the most obvious
one:**

> **The classifier's own bars are inside the outcome window.** `HELD_ABOVE` is *defined* as *"price
> did not close back below `L` in the two bars after `t`"*, and `O1` is measured from **`t+1`** —
> so bars `t+1` and `t+2` contribute to **both** the label and the outcome. **Part of the observed
> gap is mechanical, not predictive**, and the permutation test does **not** repair that: `N1`
> destroys the label/outcome association but the *observed* Δ it is compared against is already
> contaminated.

**A pre-registration that had spotted this would have measured `O1` over a disjoint window from the
start. This one did not, and that is a defect in the pre-registration, not in the data.**

### The post-hoc check — ⚠ NOT PRE-REGISTERED, AND LABELLED AS SUCH

`06_MANUAL_BACKTEST/scripts/posthoc047.py` re-measures `O1` over **`t+3` … `t+16` only** — strictly
**after** the 30-minute decision is complete, so the classifier window and the outcome window are
**disjoint**. Nothing else changes.

| arm / window | `nH` | `nB` | med `HELD` | med `BACK` | **Δ pips** | perm `p` |
|---|---|---|---|---|---|---|
| A / `W-A` (primary) | 39 | 60 | 43.20 | 16.30 | **+26.90** | 0.0005 |
| A / `W-B` | 69 | 137 | 35.50 | 12.80 | **+22.70** | 0.0001 |
| B / `W-A` | 36 | 62 | 44.20 | 23.85 | **+20.35** | 0.0044 |
| B / `W-B` | 73 | 134 | 34.30 | 16.85 | **+17.45** | 0.0001 |

⭐⭐ **THE EFFECT DOES NOT SHRINK — IT GROWS SLIGHTLY.** On the primary cell Δ moves from
**+25.75 → +26.90** pips and `p` from 0.0007 → 0.0005. Every cell stays positive and significant.

**Reading, stated conservatively:** the mechanical overlap is **not** what produces the separation.
Bars `t+1`–`t+2` were contributing almost nothing to `HELD_ABOVE`'s excursion — the extension
happens **after** the decision window, which is what the lesson actually claims (*"he will extend"*,
future tense, once the cap has passed).

⚠️ **This check is post-hoc and is reported as post-hoc.** It was run after seeing the pre-registered
result. **It cannot carry the same weight as a pre-registered arm**, and a reviewer is entitled to
discount it. **It is reported because omitting it would have been worse.**

---

## §5 — WHAT THIS DOES AND DOES NOT ESTABLISH

**It establishes:** on GBP/USD M15 over 2014–2015 development data, on both `D-031` timezone arms
and both pre-registered windows, whether price closes back below an exceeded 8–24-bar-old day-high
**within 30 minutes** separates two excursion populations by **17–27 pips at the median**, at
`p ≤ 0.004`, with the held-above median landing **inside the lesson's stated `25–50` pip band**, and
the effect **surviving a disjoint-window re-measurement**.

**It does NOT establish:**

* ⚠️⚠️ **THAT THE MAGNITUDE IS `25–50` PIPS, OR THAT `Δ` CLEARS THE 10-PIP MATERIALITY FLOOR —
  ADDED 2026-08-15, item 302.** Both are point-estimate findings and **both intervals include
  failure**: `Δ` = +25.75 bootstrap 95 % ≈ `[7.9, 38.9]` against a floor of 10, and median
  `O1`(`HELD`) = 43.20 exact CI `[30.40, 55.40]` against a band closing at 50. **The DIRECTION and
  the NON-RANDOMNESS are established; the MAGNITUDE is not.** §2a.
* ⚠ **that this is tradeable.** No spread, no slippage, no commission, no execution and no position
  sizing is modelled. `D-006` defers all of that to Phase 8. **An excursion is not a fill.**
* ⚠ **that the M formation works.** The M requires the eight-bar count *and* the *"blue tracer"*
  confirmation, and the tracer is undefined (`A-133`). **This test isolates one clause.**
* ⚠ **that the level definition is the course's.** `PT-047` §2b declared it a convention of this
  test. **A reviewer preferring a swing-pivot or peak-formation level would build a different event
  set, and that is a legitimate attack this report names rather than waits for.**
* ⚠ **anything about the `90 %` at `[00:25:34]` or the `high 80 %` at `[00:28:45]`.** Neither was
  tested; `D-009` and the third-party-anecdote problem are recorded in `Q-020` and
  `V19_INTERPRETATION.md` §2.3.
* ⚠ **anything out of sample.** The `D-035` holdout is sealed and was not touched. `W-A` and `W-B`
  **overlap** (both contain 2015), so `W-B` is a **wider-window** replication, **not** an
  independent one. `PT-047` called it *"out-of-window"*; ⚠ **that phrasing overstates it and is
  corrected here.**

---

## §6 — RUNNER / PRE-REGISTRATION DISAGREEMENTS

**One, and it is a wording defect in the pre-registration rather than a behavioural difference:**

`PT-047` §3 describes `W-B` as *"the pre-declared out-of-window replication"*. **`W-B` is
2014-01-05 → 2015-12-31 and `W-A` is 2015 — `W-B` strictly contains `W-A`.** It is a wider window,
not a disjoint one. **The runner implements exactly what §3 specifies** (`mmm_lib.WINDOWS`), so no
number changes; **the pre-registration's characterisation of that cell was wrong and is corrected in
§5, with the pre-registration left unedited** per `COMMON_PROTOCOL.md` §9 rule 7.

### ⚠ A SECOND, SMALLER DIVERGENCE — AN AMBIGUITY ID, AND WHY IT WAS **NOT** FIXED IN PLACE

`PT-047` §1a cites the undefined *"blue tracer"* as **`A-132`**. **That ID collided.** `A-131` was
already allocated by the V18 session, so this session's four ambiguity records were renumbered
`A-132`–`A-135` **after** `PT-047` was committed. The tracer is now **`A-133`**.

⚠️ **The renumber was applied to `PT-047` by a bulk edit and then REVERTED**, because
`COMMON_PROTOCOL.md` §9 rule 7 says the pre-registration is not edited — **and a one-character
"harmless" correction is exactly how that rule stops being a rule.** `PT-047` therefore still reads
`A-132`, which is now V18's record. **The correct reading is `A-133`, and it is recorded here rather
than there.**

⭐ This is the `D-047` identifier collision the project has been expecting, arriving on schedule.
**It was caught by checking the ledger tail before allocating, not after merging.**

**No other disagreement.** Every constant in the runner — `MIN_AGE = 8`, `MAX_AGE = 24`,
`HORIZON = 16`, caps of 2 and 3 bars, `BAND = (25, 50)`, `BAND_ALT = (25, 55)`,
`DELTA_FLOOR = 10`, the five `N3` conditions and the four-branch decision rule — is the value fixed
in `PT-047` before the run.

---

## §7 — REPRODUCTION

```bash
cd 06_MANUAL_BACKTEST/scripts
python3 run_pt047.py        # ~12 s; writes ../V19/data/pt047_results.json
python3 posthoc047.py       # the §4 disjoint-window check
```

Requires the HistData M1 corpus present under `06_MANUAL_BACKTEST/datasets/HISTDATA_GBPUSD_M1/raw/`
(Git-ignored; `D-011`/`D-036a`). `mmm_lib.qa_gate()` runs as a precondition on every invocation
(`COMMON_PROTOCOL.md` §1) and the run above passed it.
