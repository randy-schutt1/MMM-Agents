# SETUP SYNTHESIS — WHAT WE KNOW, AND WHAT BLOCKS IT

**Date:** 2026-08-15
**Purpose:** a map of (a) the trade setups the project holds, with the evidence tier behind each,
and (b) the specific missing pieces that stop each one from being computable — phrased as questions
a supplemental video or the owner can answer.
**Written for:** prioritising the supplemental-series ingestion that follows the 21-video corpus.

> ## ⛔ THIS FILE IS A SYNTHESIS. IT DECIDES NOTHING AND CHANGES NOTHING.
>
> - **No ledger is edited by this file.** `DECISIONS.md`, `AUTOMATION_AMBIGUITIES.md`,
>   `CONTRADICTIONS.md`, `REVIEW_INDEX.md` and `COURSE_PROGRESS.md` are unchanged.
> - **No `A-xxx` or `C-xxx` changes status. No `D-xxx` is adopted.** Where this file states a
>   status it is **reporting** one, not setting one.
> - **No `PT`/`BT` may cite this file** as a warrant for anything. It is a reading aid.
> - ⚠️ **`D-030` binds throughout.** Where the corpus is silent, this file says so rather than
>   filling the gap with a plausible reading.

---

## 0. CURRENT REPOSITORY STATE — PHASE 1 CONSOLIDATION

⭐ **This file was originally written while the knowledge was spread across five branches. Phase 1
has now consolidated those sources on `phase1/knowledge-consolidation`:**

- the integration history through V21 R2 (`PASS`), including all 21 transcript, note, screenshot,
  homework, backtest and mastery sets;
- the full owner/tooling chain through the M15 5/13 timing rule and BTMM indicator-pack survey;
- this setup synthesis.

**No source branch was deleted or rewritten.** `MMM_CURRENT_STATE.md` is the current-state authority;
the former branch table was a historical warning and is superseded by this block. The Phase 1
merge does **not** adopt any draft decision, close an ambiguity, perform the cumulative review, or
authorize the Master Specification.

### 0.1 ⛔ EVERY 2026-08-15 OWNER RULING IS IN AN **UNADOPTED DRAFT**

**The highest ADOPTED decision is `D-057`.** `D-051`, `D-055`, `D-056`, `D-058`, `D-059`, `D-060`
and `D-061` are **all drafts, none adopted, each self-marked `NOTHING HERE IS ADOPTED`.** Under
`D-047` a number belongs to whoever adopts first, so **none of those numbers is reserved.**

⭐ **Practical consequence for the supplemental series:** the owner's entry rule, stop rule, target
rule, canonical EMA set, canonical toolset and M15 timeframe are **real, recorded, and not yet
ledger law.** A supplemental video that speaks to any of them arrives *before* adoption, which is
the cheapest possible moment for Tier 1 to govern instead.

### 0.2 THE EVIDENCE TIERS USED IN THIS FILE

Taken from `SOURCING_HIERARCHY.md`, `D-039`, `D-045` and `D-052`. **They are not interchangeable.**

| Tier | Meaning | Can it close a record? |
|---|---|---|
| ⭐ **TIER 1 — `RESOLVED BY COURSE`** | The 21 videos: spoken, or printed on a slide | ✅ Yes — the only tier that can |
| **TIER 2** | `MMM-NOTES` (the Mauro seminar PDF, admitted at `D-039`) | ⚠️ Narrows; corroborates; does not outrank Tier 1 |
| **TOOLING** (`D-045`) | The owner's MT4 platform artifacts — `.tpl` files, `.ex4` inputs, alert strings | ⚠️ Eligible, admitted per-artifact; **below Tier 1** |
| **OWNER ATTESTATION** (`D-039`/`D-043`) | The owner testifying to **what the course TAUGHT** | ⚠️ Closes records the tiers cannot; **overturnable by a later Tier 1 statement** |
| **OWNER EMPIRICAL PREFERENCE** (`D-052`) | The owner stating **what he does / what the instrument does** | ⛔ **Not course evidence.** Records practice, not doctrine |
| ⛔ **`DO NOT CODE`** | The project's block. Not a confidence rating — a prohibition | — |

⚠️ **The distinction that matters most for the supplemental series:** *OWNER ATTESTATION* is a claim
about the corpus and can be **upgraded to Tier 1 if a video says it on camera.** *OWNER EMPIRICAL
PREFERENCE* is a claim about the owner and **cannot be upgraded by anything** — but a Tier 1
statement on the same subject **overrides it** under `SOURCING_HIERARCHY.md` §3.1/§3.4.

---

# PART 1 — THE ESTABLISHED SETUPS

## 1.1 ⭐ THE COURSE'S OWN ENUMERATION — V20's NINE-PATTERN ANSWER KEY

⭐⭐ **This is the single most valuable structural fact in the corpus for this purpose: the course
enumerates its own pattern set exactly once, and it is complete.** V20 slide `38:15`, printed, and
read out at `[00:38:09]`–`[00:38:41]` — the answer key to a test the instructor sets in the lesson
(*"We just covered a bunch of patterns. Write them down."*).

> **ANSWERS**
> 1. Half–A-Batman
> 2. Upside Down Half-A-Batman
> 3. Evening/Morning Star
> 4. Spike To The Low (vector)
> 5. Spike To The High (vector)
> 6. RR Tracks
> 7. 2 Pins High/Low M or W
> 8. Extended Consolidation After A Pattern
> 9. Outside Structure

⭐ **The slide is TIER 1 and it settles one reading the audio leaves open:** item 7 is **one** item,
not two (`04_SCREENSHOTS/V20/INDEX.md` §8). ⭐ **Item 9 is the concept V20 added that night**, and
the **audio trails off before naming it — only the pixels carry it.**

⚠️ **Enumeration is not definition.** The course names nine patterns and defines four of them
(outside structure, spike, railroad tracks, and — from V19 — the M/W second leg). **The list is the
scope of the supplemental series' job, not a claim that the job is done.**

---

## 1.2 THE SETUP TABLE

**Legend:** ✅ = computable today · ⚠️ = defined but not computable · ⛔ = `DO NOT CODE`

| # | Setup | One-line description | Best evidence tier | Computable? |
|---|---|---|---|---|
| **S1** | **Outside structure** | The aggressive **single-leg** vector move that sets the HOD/LOD (and the HOW/LOW); *"all trap moves will come in as an outside structure"*; at the chart's extreme it is *"an absolute sign of reversal"* | ⭐ **TIER 1 — V20 slide `00:25` + `[00:00:00]`–`[00:01:45]`** | ⛔ `A-033` — the *structure* whose outside is meant is never bounded |
| **S2** | **M formation** (tops ⇒ short) | Two-leg top: leg 1 induces longs, the centre triggers stops, **leg 2 may exceed leg 1 but must close back below within ~30 min** | ⭐ **TIER 1 — V19 slide (written on camera at `23:25`) + `[00:22:40]`** | ⛔ `A-011` — the **second** leg has a printed rule; **the FIRST leg has none** |
| **S3** | **W formation** (bottoms ⇒ long) | Exact mirror of the M; *"the W is exactly the opposite"*; leg 2 may go slightly below leg 1 but must close back above | ⭐ **TIER 1 — V19 slide (`50:34`) + `[00:46:38]`** | ⛔ Same as S2, **plus `C-029`: the W slide says `30 to 45 m`, the M slide says `30M`** |
| **S4** | **Half-A-Batman** | An M/W **with its second leg missing** — the first move already filled the dealer's book, so no return is needed; *"he shifts the zone without going back"* | 🟡 **TIER 2 — `MMM-NOTES` p.19** (*"Anatomy of the Half Batman Pattern"*), logic only · TIER 1 names it (V02 `[00:20:37]`, V19 `[00:50:46]`, V20 item 1) | ⛔ `A-022` — **the logic is recovered, the geometry is not.** No shape, no invalidation, no count |
| **S5** | **Upside-Down Half-A-Batman** | The long-side mirror of S4 | ⚠️ **TIER 1 by NAME ONLY** — V20 answer key item 2 | ⛔ **Never described anywhere.** Inherits every S4 gap |
| **S6** | **Evening / Morning Star** | Single-bar-family reversal signal at a structure extreme | ⚠️ **TIER 1 by NAME ONLY** (V20 item 3) · ⛔ the only attempted definition is **guest material and self-contradictory** — `A-046` | ⛔ `A-046` — V05 assigns **opposite names to the same description 20 seconds apart**, and `D-025`/`D-033` guest rules apply |
| **S7** | **Spike to the high / low (vector)** | *"An aggressive change in price usually following a news announcement"*, used to clear stops or shift the trading zone | ⭐ **TIER 1 — V20 `[00:20:23]`, slide `20:30`** | ⛔ **Explicitly NEVER an entry** — `[00:26:06]` *"Is there a way to use a spike for an entry? **No, avoid it.**"* Direction is `A-137` (unobservable) |
| **S8** | **Railroad tracks (RR)** | ⭐ **Defined as a FUNCTION, not a shape:** *"a 30-minute structure where the market makers trigger the stops, shift the zone, and set the high or low all in one move"* | ⭐⭐ **TIER 1 — V20 `[00:25:50]` + `[00:27:02]`, stated twice, plus *"That's the definition. By definition."*** · geometry from V19 `[01:02:45]` *"30 minutes, 15 in, 15 out"* | ⚠️ **The strongest definition in the corpus** — but *"trigger the stops"* and *"shift the zone"* are both undefined operations |
| **S9** | **2 Pins High/Low M or W** | A two-pin variant of the M/W at the extreme | ⚠️ **TIER 1 by NAME ONLY** — V20 slide `38:15`, one line | ⛔ **Never described.** The slide settles that it is one item; nothing settles what it is |
| **S10** | **Extended consolidation after a pattern** | The long chop that follows an outside structure — *"a powerful extended move is ALWAYS followed by level three behaviour"*, and *"that's part of the pattern"* | ⭐ **TIER 1 — V20 `[00:42:51]`, `[00:43:02]`, slide `39:45`** | ⛔ `A-138` — `L3` has three named components (**MA separation, chop going nowhere, an extreme repeatedly approached and not broken**) and **not one of them is quantified** |
| **S11** | **Stop hunt** | The move that takes out stops at a level and returns | ⚠️ **TIER 1 by USE** — pervasive; ⛔ **the instructor has never defined it**, and two guest presenters define it **incompatibly** (`C-006`) | ⛔ `A-009`, `A-049` — **no discriminator between a stop hunt and a trap move exists anywhere** |
| **S12** | **Trap move** | The move that traps traders at the worst price; *"LOY and HOY are major trap moves — do not trade these as breakouts"* | ⭐ **TIER 1 as a rule** (V20 slide `39:45`); ⛔ undefined as an object | ⛔ `A-002`, `A-049` |
| **S13** | **Safety trade** | A named trade type with a printed seven-point checklist (V17) | ⭐ **TIER 1 — V17 printed checklist** | ⛔ **The homework completes it to TWO of seven on real data** (`05_HOMEWORK/V17/` §1). Five points rest on `peak formation`, `M`/`W`, *"spike"*, the TDI bands or an exit rule the corpus does not contain. `A-034`, `A-078`, `A-130` |
| **S14** | **Hi-Lo** | Entry at or within *"a few pips"* of the day's high/low; called *"the holy grail of daily trading"* in print | ⭐ **TIER 1 — V07 `[00:07:13]`–`[00:07:17]`, printed twice** | ⛔ `A-056` — ⭐ **the OBJECT is arithmetic; the missing thing is a method of identifying the extreme in real time**, and it is deferred to another presenter (*"Jim"*) and to the paid DMR |
| **S15** | **The one-third entry** | ⭐ **`entry = high − (candle range ÷ 3)`**, worked at 100 → 33 pips; mirrored for a long | ⭐⭐ **TIER 1, ARITHMETICALLY COMPLETE — V20 `[00:29:14]`–`[00:29:53]`** | ⚠️ **`A-139` — the arithmetic is complete; WHICH CANDLE is never stated in words.** Two contextual candidates (the RR-track bar, the zone-shifting bar) and they may be the same bar |
| **S16** | **Dinosaur pattern** | A weekly shape that can **override the running level count** | ⚠️ **TIER 1 by name + consequence** — V09 `[00:34:44]` | ⛔ `A-071` — drawn live in Paint, described only by its consequence; *"we all know what that is"* points at **material outside this corpus** |
| **S17** | **"A pattern"** | The expectation at level one, contrasted with a second leg | ⛔ **UNVERIFIED — may not be a real term.** One occurrence, V07 `[00:14:07]` | ⛔ `A-057` — possibly ASR garble for *"an M pattern"*. **Deliberately NOT merged with `A-047`** |
| **S18** | **33 trade / 22 trade / 3333 trade** | Named trades tied to legs/levels | 🟡 **33:** Tier 2 decodes 3 levels over 3 days + an intraday L3. ⭐ **22:** V02 explicitly describes a second leg of a second leg at a repeated level. **3333:** name only | ⛔ **33** remains blocked by undefined `level` segmentation. **22** remains blocked for raw-chart computation by `second leg` anatomy and the unquantified *"slightly"* overshoot; V15's hyphenated use is not independently defined. **3333** has no definition. The former statement that 22 had *"zero definitions"* was false and is superseded here |
| **S19** | **High / Low Trainer grid** | An **order-placement tool**: market order + two pendings at **+20/+40**, hard stop on all three, cycle `30+50+70 = 150`, *"most often +80"*, `MAX RISK 5%` | ⭐ **TIER 1 — V21, described, spec photographed (`31:25`, handwritten, dated `1-27-2010`), and INSTALLED ON CAMERA** | ⛔⛔ **`A-141` — THE ARTIFACT IS NOT IN THIS REPOSITORY.** Four filenames are recovered: `&sm_Buy_Order_Trainer`, `&sm_Buy_Order_Trainer_Pct`, `&sm_Sell_Order_Trainer`, `&sm_Sell_Order_Trainer_Pct`. No code, trigger, fill/management logic, exit logic or parameter dialog. ⭐ The course shipped a complete implementation and the executable copy did not reach the project |

---

## 1.3 ⭐ THE OWNER'S OWN TRADE TEMPLATE — THE ONLY COMPLETE ONE IN EXISTENCE

⭐⭐ **Read `D-055` and `D-058` together and the project holds ENTRY + STOP + TP1 + TP2 from one
coherent source. Twenty-one lessons did not produce that set.** ⛔ **Both are unadopted drafts at
`OWNER EMPIRICAL PREFERENCE` — the owner's practice, NOT course-verified.**

| Component | Rule | Tier | Computable? |
|---|---|---|---|
| **Direction** | From the **M/W setup** — M at tops ⇒ short, W at bottoms ⇒ long | ⚠️ **OWNER EMPIRICAL PREFERENCE** (`D-055` §4A) | ⛔ **Blocked on `A-011`** |
| ⭐⭐ **Entry trigger (tier 1)** | ⭐ **On M15, `EMA(5, close)` crosses `EMA(13, close)`; entry at the NEXT BAR'S CLOSE** | ⚠️ **OWNER EMPIRICAL PREFERENCE** (`D-058` §4.1F, three rulings on 2026-08-15) | ✅⭐ **YES — the project's FIRST fully-specified executable entry trigger in 21 lessons** |
| **Entry (tier 2)** | When *"confidence and probability is on your side"*: on the **second-leg** candle signal — hammer, railroad tracks, doji, **etc.** | ⚠️ OWNER EMPIRICAL PREFERENCE | ⛔ **Least codable sentence in either rule.** No threshold; *"etc."* is an open list |
| **Entry (tier 3, *"safest"*)** | A **LIMIT order on the close of the outside bar** — the only tier with an order type | ⚠️ OWNER EMPIRICAL PREFERENCE | ⛔ *"Outside bar"* undefined by course **and** owner; and *"limit on the close"* is itself two different orders |
| **Stop** | **25 pips beyond the perceived extreme** = beyond **where the M/W forms** | ⚠️ OWNER EMPIRICAL PREFERENCE (`D-055` §4A) | ⛔ **`A-142`** — resolves into `A-011` |
| **TP1** | **30 pips** | ⚠️ OWNER EMPIRICAL PREFERENCE | ⚠️ Inside V19's Tier 1 `25 to 50` band; **V19 never says 30** |
| **TP2** | **50 pips** | ⚠️ OWNER EMPIRICAL PREFERENCE | ⭐ Sits **exactly on the upper bound** of V19's Tier 1 band; `25 → 50` is exactly V06's *"2 to 1"* |
| **Position sizing** | ⭐ **Fix the risk %, let the stop distance set the lot size, and do not skip a valid structure because its stop is wide** | ⭐ **TIER 1 — V19 `[00:58:20]`–`[01:00:12]`** | ✅ **Computable given a stop distance.** ⚠️ The `2%` constant itself is `A-065` |

> ### ⛔⛔ THE MOST IMPORTANT SENTENCE IN THIS FILE
>
> **The 5/13/M15 cross is a TIMING GATE, NOT A SIGNAL — on the owner's own wording:**
> *"**If I decide to take an entry** … then I need to **wait for** the ema cross."*
>
> | | |
> |---|---|
> | ✅ **COMPUTABLE** | *"Given a decision to go long/short on M15, WHEN do I enter?"* — **fully determined** |
> | ⛔ **NOT COMPUTABLE** | *"Should I enter, and in which direction?"* — **blocked on `A-011`** |
>
> ⭐ For a setup-detection agent this is precisely the useful half. **Do not describe it as a
> complete entry signal.**

### 1.3.1 ⚠️ FOUR INCOMPATIBLE ENTRY GEOMETRIES NOW COEXIST

**This count is itself a finding about the corpus** (`D-058` Part 5):

1. `A-090` — the board drill: **10 pips inside**, or **5–8** (the record contradicts itself)
2. `A-139` — V20: **`high − (range ÷ 3)`**
3. V19 — **close below the blue tracer** (`A-133`, line undefined)
4. `D-058` tier 1 — the owner: **the close of the bar after the 5/13 cross**

⛔ **No source reconciles them, and `D-052` §3.5 bars treating (4) as answering (2)** — they are
different categories of claim, not a contradiction to adjudicate.

---

## 1.4 THE SUPPORTING INSTRUMENTS — WHAT CAN AND CANNOT BE COMPUTED

The owner scoped the toolset to **four instruments** (`D-059`, draft): *"Worktime ribbon, TDI, EMAs,
ADR. I believe that's it."*

| Instrument | State |
|---|---|
| ⭐ **EMAs** | ✅ **`5 · 13 · 50 · 200 · 800`** ruled canonical (`D-061`, draft; re-affirms `D-043`). `MMM.tpl`'s `4 · 10 · … · 3200` is **not** canonical. ⭐ **The tooling audit came back CLEAN — every project tool already plots the canonical set.** ⚠️ Nicknames: `mustard=5`, `ketchup=13`, `water=50` are **OWNER ATTESTATION**; `mayonnaise=200` and `blueberry=800` are **TIER 1** |
| **TDI** | ⚠️ `MMM_TDI.txt` is the **primary instrument** (`D-053`, ADOPTED). Band basis = **RSI line** (`D-052`, ADOPTED, `OWNER EMPIRICAL PREFERENCE` — ⚠️ **Tier 1 V14 and Tier 2 p.45 both say the market base and are overridden**). ⛔ **`A-086`: the band PERIOD is never stated at any tier — `DO NOT CODE`**, which makes *shark fin* (`A-032`) and *blood in the water* (`A-031`) **uncomputable even though both are `RESOLVED BY COURSE` as to MEANING** |
| **Worktime ribbon** | ⚠️ `D-056` (draft). Clock ruled **EST** by the owner — ⛔ **and the ruling makes the gap SHARPER: the two best anchors demand offsets exactly one hour apart** (proposed `C-032`, `A-105`). ⚠️ The owner's description (*"sessions and previous highs and lows"*) **does not match his own chart** — the ribbon draws sessions only |
| **ADR** | ⛔ `D-051` (draft) / `A-100` — **taught for a whole lesson and its LOOKBACK is never stated.** `C-011`: notes say two weeks, corpus says two days. `C-022`: the markers *"creep"* AND are *"not repaint"* — a definitional conflict |
| **Pivots** | ⚠️ Owner: *"Pivot points aren't how I trade. It's an added bonus."* ⭐ **Tool-building priority LOW; `A-101` NOT closed and course-study priority untouched** — V16 is an entire lesson on them |

---

## 1.5 WHAT HAS ACTUALLY BEEN TESTED

⚠️ **Only one setup-level claim in the corpus has ever returned a confirmation, and it carries
caveats.**

| Test | Subject | Verdict |
|---|---|---|
| **PT-047** | V19's M/W **second-leg 30-minute close-back rule** | ⭐ **CONFIRMED** — Δ = +25.75 pips, p = 0.0007, 8 of 8 cells. ⚠️ **Both legs of the `CONFIRMED` gate lie inside the 95% interval of failing** (item 302), and the event definition is **the TEST's convention, not the course's** |
| **PT-048** | V20 swing-scale claim | ⛔ **REFUTED** at the pre-registered `k=3` scale — median 30.10 `[28.80, 31.25]`, refuted on all four cells |
| **PT-049** | V20's **one-third entry** (`A-139`) | ⬜ **Pre-registered, runner not yet written** |
| **PT-050** | V21's **High/Low Trainer grid** | ⚠️ **FRAGILE, reported as a null** — median move 29.70 `[23.20, 39.60]` against the claimed *"about 50"*. ⭐ **The event IS real — roughly double a random window on every cell — it just is not 50** |

⭐ **`CONCEPT_INDEX.md` still holds ZERO promoted concepts after 21 lessons**, and V19 is the
closest miss: a rule that was spoken six times, mirrored, written into the deck on camera, and
**tested and confirmed** — and it is still refused, because *"a statistical confirmation of this
session's operationalisation is not the course's definition."*

---

# PART 2 — THE PRIORITISED GAP LIST

**Ranked by: (how many setups it unblocks) × (how cheap it is to answer).**
Each item is phrased as a question a supplemental video or the owner could answer directly.

---

## ⭐⭐⭐ P0 — THE ONE THAT UNBLOCKS THE MOST

### **G1 — `A-011`: HOW IS THE FIRST LEG OF AN M/W IDENTIFIED?**

> **Question:** *"When you look at a chart, what makes a move the FIRST LEG of an M or a W — as
> opposed to just a move? How many candles, what size, what has to have happened before it?"*

⭐⭐ **This is the highest-value single answer available to the project, and it is not close.**

**What it unblocks, all at once:**

- **S2/S3 (M and W)** — the direction source for the entire owner template
- **`D-058` tier 1** — the M15 5/13 trigger is computable **but has nothing to tell it which way to
  trade**; this is the missing half
- **`D-055`** — the stop anchor is *"where the M/W forms"* (`A-142`), which **resolves into this**
- **S4/S5 (Half-A-Batman)** — defined as *"an M/W with its second leg missing"*; you cannot detect a
  missing second leg without a first
- **S9 (2 Pins High/Low M or W)**
- **`A-007`** (*"second leg"*), **`A-044`** (what makes a second leg a *"nameable pattern"*)

**What is already known and must not be re-asked:** ⭐ V19 **prints** the SECOND-leg rule for both M
and W — *"can be slightly above the first, but must close below within 30M"* — and `PT-047` confirmed
it. **V17 adds two constraints:** *"eight candles or above"* (`A-010`) and *"you don't have a W in
price, you have a W in the CLOSING of price"* (`A-011`). ⛔ **The pair now has a series, a floor and
a time cap — and still no rule for where it starts.**

---

## ⭐⭐ P1 — CHEAP, AND EACH CLEARS A WHOLE SETUP

### **G2 — `A-086`: WHAT IS THE TDI VOLATILITY BAND'S LOOKBACK PERIOD?**

> **Question:** *"What number goes in the volatility-band period box on the TDI?"*

⛔ **One integer.** It is the **only** thing blocking `A-031` (*blood in the water*) and `A-032`
(*shark fin*) — **both of which are already `RESOLVED BY COURSE` as to MEANING.** Two named,
course-defined signals are uncomputable for want of one scalar.

⚠️ **The owner has already said *"the TDI band is fine"* and that he finds both patterns on his
chart** (`D-057`, ADOPTED) — ⛔ **and that did not move `A-086`, because he stated no number, and a
human recognising a shape is not a machine criterion.** ⭐ **`D-057` §5 has a yes/no question already
drafted and owed:** is he looking at `MMM_TDI.txt` on TradingView or at his MT4 `!SM_TDI`, and may
`bandLen = 34` / `bandMult = 1.6185` be relabelled `OWNER-CONFIRMED WORKING`?
⚠️ **Even a "yes" upgrades a tag on a tool, not a record in the register.**

⚠️ **And a second discrepancy is open and owed:** the primary instrument ships `bandMult = 1.6185`
(a **Tier-3 guess**) while **V14 `[00:45:09]` says *"two standard deviations"* unhedged at Tier 1**
(`D-052` consequence 6, `D-053` consequence 5).

### **G3 — `A-133`: WHAT IS THE "BLUE TRACER"?**

> **Question:** *"The blue tracer — is it a moving average, and what period? Or is it a level?"*

⛔⛔ **DIMENSION B IS NOW PERMANENTLY BLOCKED ON THIS.** The term is spoken **15 times in V19 alone**,
it carries V19's **only** entry rule (*"he closed below the blue tracer"*), and **the course ended
without ever defining it.** Eight lessons blocked.

⚠️ **V21 produced a NEAR-MISS that would have closed it wrongly** — the committed grid reads *"the
light blue tracer **in** the ADR line"* (an apposition, which would define it); **the independent ASR
decode reads `AND`** — a list of two. **Caught before it reached any artifact.**

⭐ **This is now answerable ONLY by the owner or by supplemental material.** A `D-045`-class artifact
that names the line would also do it.

### **G4 — `A-141`: WHERE IS THE HIGH / LOW TRAINER SCRIPT?**

> **Question:** *"Do you still have the MT4 scripts folder from boot camp? There should be four
> scripts — the one recommended is 'by order percent'."*

⭐⭐ **The single highest-value ACQUISITION available to this project**, and it is a file-transfer
problem rather than a knowledge problem. **The course shipped a complete, working implementation and
the copy did not reach this repository.**

⭐ V21 R1 found and V21 R2 independently re-read **four script filenames** in the committed frame:
`&sm_Buy_Order_Trainer`, `&sm_Buy_Order_Trainer_Pct`, `&sm_Sell_Order_Trainer`, and
`&sm_Sell_Order_Trainer_Pct`. **The names are recovered; the executable behavior is not.**
⛔ **`D-030` bars reconstructing the tool from the known figures** (`+20/+40`, `30+50+70`, `MAX RISK
5%`): a grid whose spacing is known but whose entry trigger, fill logic and exit are not **is not the
tool**, and a reconstruction would acquire the tool's authority without its behaviour.

---

## ⭐ P2 — THE OWNER'S OWN RULE, FINISHED

**Five questions, all already drafted in `D-058` §7 and `D-055` §7, none yet answered.**

### **G5 — What makes *"confidence and probability"* on your side?**
> Even a rough checklist converts tier 2 of the entry ladder from **unevaluable** to **testable**.
⛔ Currently *"the least codable sentence in either owner rule."* And note ⭐ **the corpus has the
same gap from the opposite end**: `A-115` states two entry DISQUALIFIERS with no thresholds.

### **G6 — What is an *"outside bar"* to you?**
> Higher high **and** lower low than the prior bar, or something else?
⛔ **The course never defines it and neither does the owner.** Importing the retail definition is the
`A-082` error. This blocks tier 3 — **the tier he calls safest.**

### **G7 — Tier 3: is the limit AT the close price of the outside bar, or placed at that moment at some other level?**
> **These are different orders**, and every backtest of tier 3 depends on which.

### **G8 — Are the three entry tiers alternatives or a sequence?**
> Do you take whichever presents, or wait for the safest and drop to tier 1 only if it never comes?

### **G9 — Are TP1 and TP2 a scale-out or alternatives?** And **do the price targets REPLACE the duration-based exits?**
> ⭐ The second half is large: `C-001`, `C-016` and `A-121` all measure exits in **days**; the owner's
> are in **pips**. If price targets are what he actually exits on, three long-standing records become
> answerable. ⛔ `D-055` §3.5 refuses to assume it — **a price rule is not a time rule.**

### **G10 — Confirm the two declared conventions on the cross** (`D-058` §4.1H)
> (a) that the awaited cross is the one **in the trade's direction**; (b) that the EMAs run on
> **close**. **Neither blocks; both must be declared rather than silently assumed** under `D-027`.

---

## P3 — THE STRUCTURAL DEFINITIONS THE COURSE OWES

### **G11 — `A-033`: where is the boundary of the "structure" in "outside structure"?**
⚠️ V04 gave the first definition attempt (*"a spike vector to the high and then trade off that
level"*) and V20 the fullest — **both describe the dealer's BEHAVIOUR, not the chart BOUNDARY a
machine would test.** The most likely referent is the week's accumulation high/low; **that reading is
coherent and is not stated.**

### **G12 — `A-049`: what distinguishes a stop hunt from a trap move?**
⛔ **A clean discriminator has never been stated.** The instructor has never defined *stop hunt* at
all, and **two guest presenters define it incompatibly** (`C-006`). This blocks S11 and S12 and half
of S13's checklist.

### **G13 — `A-138`: quantify Level 3.**
> What moving-average **periods**? What **separation** distance? How **long** must the chop run? How
> many times is *"several"*?
⛔ Three named components, **zero quantified**, and the printed claim carries *"Always"*. Blocks S10.

### **G14 — `A-139`: WHICH candle in the one-third entry?**
⭐ **The smallest gap on the list** — the arithmetic is complete, the bar is not named. Two candidates
(the railroad-track bar; the zone-shifting bar) and they may be the same bar. **`PT-049` will test it
under a declared convention** — ⛔ **and a declared test convention is not a course rule.**

### **G15 — `A-046`: which is the shooting star and which the evening star?**
⛔ V05 assigns **opposite names to the same description ~20 seconds apart**, and it is **guest
material**, so it could not define a course term regardless. Blocks S6. ⚠️ **Do NOT repair by
selecting whichever clause matches expectations.**

### **G16 — S5 and S9: describe the Upside-Down Half-A-Batman and the "2 Pins High/Low M or W".**
⛔ **Named on the answer key and described nowhere in 21 lessons.** These are the two items in the
course's own enumeration with **literally zero content behind them.**

### **G17 — `A-137`: how do you OBSERVE dealer open volume?**
⚠️ **An unusual shape worth distinguishing from ordinary vagueness: the rule is not imprecise, it is
UNMEASURABLE with what the student has.** V20 makes open volume the **cause** of spike direction and
supplies no feed, broker report, proxy or threshold. **The only usable residue — public net long ⇒
dealer net short — predicts nothing on its own.**

### **G18 — `A-134` / `C-029` / `C-030`: settle the second-leg time cap.**
⛔ V19 says it **four ways** (30 / 45 tops / 30–45 / 30-to-an-hour), and **the two on-camera slide
edits DISAGREE — the M slide says `30M`, the W slide says `30 to 45 m`.** V20 then says **`30 minutes
or less`, flat**. `C-030`'s disposition is `PROVISIONAL`. ⭐ **One sentence settles it, and `PT-047`
already runs 30 as primary and 45 as a pre-declared robustness arm — both confirm.**

### **G19 — `A-056`: how do you identify the high or low of the day IN REAL TIME?**
⭐ Note the asymmetry: **the object is arithmetic, not ambiguous.** What is missing is the *method*,
which is the entire content of the claim — and the course defers it to another presenter (*"Jim"*)
and to the paid DMR programme. ⚠️ **If the supplemental series contains a Jim presentation, this is
what to watch for**, and `D-025`/`D-033` guest-evidence rules will govern how it can be used.

### **G20 — `A-100` / `C-011` / `C-022`: what is the ADR lookback, and do the markers repaint?**
⛔ A whole lesson on the ADR and **its lookback is never stated.** Tier 2 says two weeks, the corpus
says two days. And the markers are stated to *"creep up or creep down to fit price action"* **AND**
to be *"not repaint"*, **twenty seconds apart** — a definitional conflict that blocks any
implementation.

### **G21 — `A-019` / `A-105` / `A-131`: fix the session clock.**
⭐ V20 finally gives a clock time **with a named timezone** — *"three thirty, four o'clock **New York
time**"* — the first in the corpus. ⚠️ **And the owner's EST ruling made the ribbon gap SHARPER, not
smaller: the two best anchors now demand offsets EXACTLY ONE HOUR APART.** V18 supplies **no** clock
time for any session at all.

---

# PART 3 — ⭐ THE WATCH LIST FOR THE SUPPLEMENTAL SERIES

**Items where the owner believes course material exists that this corpus does not contain. Flagged
so an ingestion session recognises them on camera and reconsiders their evidence tier.**

## 3.1 ⭐⭐ THE "5/13 CROSS ENTRY" — THE OWNER BELIEVES THIS IS COURSE-SOURCED

> **Owner, 2026-08-15:** *"5/13 cross entry"* · *"So this is a universal rule. If I decide to take an
> entry in any time frame then I need to wait for the ema cross. Based on the strategy now with MMM
> then it's M15."*

**Currently recorded as:** ⚠️ **`OWNER EMPIRICAL PREFERENCE`** (`D-058`, draft) — *the owner's own
practice, NOT course-verified.*

⛔ **AND THE REASON THAT TIER WAS CHOSEN IS A MEASURED NEGATIVE, NOT AN ASSUMPTION.** `D-058` §3.3:

> **No lesson in V01–V21 states an entry at "the close of the candle after an EMA cross."** A sweep
> of `10_AMBIGUITIES/`, `11_CONTRADICTIONS/` and `03_LESSON_NOTES/` for EMA-cross entry triggers
> returns **nothing**. The corpus's EMA material is about **which averages exist** (`A-020`, `C-010`,
> `D-043`) and **confluence** (*"an M formation off the Mayo"*, V08 `[00:22:13]`) — **never a cross
> as a trigger.**

⭐ **The owner states he is confident Steve covers this in supporting material, and that it is likely
in the supplemental series rather than his own invention.** ⭐⭐ **That is a testable prediction, and
if it lands it is the most consequential single upgrade available:**

| If found on camera | Consequence |
|---|---|
| ⭐⭐ **A supplemental video states the EMA-cross entry** | **The status moves from `OWNER EMPIRICAL PREFERENCE` to `RESOLVED BY COURSE`** — the project's first course-verified executable entry trigger, rather than the owner's |
| ⭐ **It states the pair (5/13) too** | `A-143`'s `[PAIR]` row gains a **Tier 1** warrant instead of an owner one |
| ⭐ **It states the timeframe** | Same for `[TIMEFRAME]` — ⚠️ and **note the owner's own templates split 3–2 the other way: `MMM.tpl`, `MMM INDICES.tpl`, `RS5P.tpl` are all saved on H1** |
| ⭐⭐ **It states the DIRECTION rule** | ⭐ **This is the one that would change the most.** If a video says the cross must agree with the M/W — or worse, that the cross *chooses* direction — **the "timing gate, not a signal" finding (§1.3) is directly affected**, and the `A-011` dependency may narrow |

⚠️⚠️ **AND THE OPPOSITE OUTCOME IS ALSO A RESULT AND MUST BE REPORTED AS ONE.** If the supplemental
series is ingested in full and **still** never states an EMA-cross entry, that **strengthens**
`D-058` §3.3's negative and confirms the `OWNER EMPIRICAL PREFERENCE` classification was right.
**A negative here is evidence, not a failure to find something.**

⛔ **What an ingestion session must NOT do:** upgrade the tier because the owner *expects* the
material to exist. `D-039`'s error — *treating the admission of a source as a reading of it* — is
the precise failure mode, and this project has already made it once.

## 3.2 THE OTHER "IT'S COVERED SOMEWHERE" POINTERS ALREADY IN THE LEDGERS

**Watch for all of these; each has an explicit in-corpus pointer to material this repo lacks.**

| Item | The pointer | Tier if found |
|---|---|---|
| **Hi-Lo entry method** (`A-056`, S14) | *"Jim… covers some of that on how he gets into those entries"* (V07 `[00:38:26]`); the DMR curriculum prints a **`Hi/Lo Drill`** week | ⚠️ **Guest material — `D-025`/`D-033` govern.** Would still be a large advance |
| **The dinosaur pattern** (`A-071`, S16) | *"**we all know what that is**"* — an appeal to teaching **outside this corpus** | ⭐ Tier 1 if the instructor |
| **The DMR programme** (`A-042`) | The operative detail is **repeatedly deferred to the DMR**, which this library does not contain | ⭐ Tier 1 if the instructor |
| **Flashcards** (`A-082`) | Standing required work, referred to as **already existing**, never specified. V20 assigns *"Develop Flash Cards Of These Exact Sets"* | ⭐ Tier 1 |
| **The *"four plays"*** (V20 `[00:42:43]`) | *"they only got four plays"* — ⛔ **never enumerated** | ⭐ Tier 1 |
| **The second bootcamp cycle** (`A-104`) | A second cycle was planned for **autumn 2012** and this corpus holds **nothing** from it | ⭐ Tier 1 — ⚠️ **a likely source of the supplemental series** |
| **High/Low Trainer scripts** (`A-141`, S19) | *"four scripts in there"*, *"the one I recommend using is by order percent"* | ⭐ **TOOLING** (`D-045`) — the artifact itself |

---

# PART 4 — THE ONE-PAGE ANSWER

**What is solidly established:**

1. ⭐ **The course's own nine-pattern enumeration** (V20 slide `38:15`, Tier 1) — the scope is known.
2. ⭐ **Four patterns are genuinely DEFINED at Tier 1**: outside structure, spike, railroad tracks
   (as a *function*), and the M/W **second-leg** rule.
3. ⭐⭐ **Two rules are arithmetically COMPLETE**: V20's `entry = high − (range ÷ 3)`, and V19's
   constant-risk position sizing.
4. ⭐⭐ **One entry TRIGGER is fully computable**: M15 `EMA(5)` × `EMA(13)`, entry at next bar's close
   — **owner-ruled, not course-verified, and a timing gate rather than a signal.**
5. ⭐ **A complete owner trade template exists** — entry, stop, TP1, TP2 — **from one coherent
   source, at the weakest tier, with its direction input blocked.**

**What blocks everything else, in one sentence each:**

- ⛔ **`A-011`** — no rule identifies the FIRST leg of an M or W, so nothing can decide *whether* or
  *which way* to trade. **Answer this first.**
- ⛔ **`A-086`** — one missing integer makes two course-defined TDI signals uncomputable.
- ⛔ **`A-133`** — the blue tracer is undefined and the course has ended; **permanently blocked**
  without owner or supplemental input.
- ⛔ **`A-141`** — the one complete implementation the course shipped is not in this repository.

**And the thing to watch for on camera:** ⭐⭐ **the 5/13 EMA cross entry.** The owner believes Steve
teaches it; **21 lessons demonstrably do not.** If the supplemental series states it, the project's
first executable entry trigger stops being the owner's and becomes the course's.

---

*Built by reading `00_SYSTEM/DECISIONS.md` (through `D-057` adopted), the consolidated
`D-051`/`D-055`/`D-056`/`D-058`/`D-059`/`D-061` draft files, `10_AMBIGUITIES/
AUTOMATION_AMBIGUITIES.md` (`A-001`–`A-143`), `11_CONTRADICTIONS/CONTRADICTIONS.md` (`C-001`–
`C-031`), `18_REVIEW/REVIEW_INDEX.md`, `00_SYSTEM/COURSE_PROGRESS.md`,
`08_CONCEPT_LIBRARY/CONCEPT_INDEX.md`, and the V19/V20/V21 source notes and screenshot
indices. No external summary was used.*
