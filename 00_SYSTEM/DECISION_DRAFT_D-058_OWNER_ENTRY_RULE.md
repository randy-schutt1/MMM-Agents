# DECISION DRAFT — `D-058` (proposed) — THE OWNER'S ENTRY RULE

> ## ⛔ NOTHING IN THIS FILE IS A DECISION. NOTHING HERE IS ADOPTED.
>
> One **draft**, prepared on the owner's direct first-person statement of how he enters. It is
> written to the point where the owner can answer **yes / no / edit**, and the approved text can be
> appended to `DECISIONS.md` unchanged. It is the **companion to `D-055`** (the owner's stop and
> targets) and is recorded at the **same tier**, with the same discipline: **an attestation is
> admitted for what it IS, never promoted to what would be more useful.**
>
> Until the owner rules:
>
> - **`DECISIONS.md` is unchanged.** No `D-058` exists.
> - **`A-139`, `A-133`, `A-017`, `A-115`, `A-090`, `A-056`, `A-011` are untouched.** Nothing
>   course-sourced closes here.
> - **No `PT`/`BT` may cite this file.**
>
> **Numbering, verified against integration at `19e6c2a` rather than assumed.** Highest **adopted**
> entry is **`D-057`**; `D-051`, `D-055` and `D-056` are unadopted drafts holding their numbers. This
> draft therefore claims **`D-058`**. ⚠️ `D-055`/`D-056` are **drafts on integration, not adopted
> entries** — a session adopting this one must re-check, because `D-047` gives the number to
> whoever adopts first.

---

## 0. THE ONE-PARAGRAPH SUMMARY, INCLUDING THE PART THAT DOES NOT GO THE OWNER'S WAY

The owner has stated **a three-tier entry ladder** — a default trigger, a confidence-conditional
upgrade, and a "safest" variant — in the first person, unprompted. ⭐ **Together with `D-055` this
is the first time the project holds an ENTRY, a STOP and TWO TARGETS from one coherent source.**
Twenty-one lessons have not produced that set.

⛔ **And it is not executable, for the same reason `D-055` was not: every tier is anchored to an
object the corpus does not construct.** *"The EMA cross"* does not say **which two EMAs** — and this
project's own records disagree about which periods even exist (`D-043`'s 5/13 against `MMM.tpl`'s
4/10). *"Confidence and probability on your side"* is a subjective gate with no threshold.
*"Second leg"* runs into `A-011`. *"The outside bar"* is never defined by the course. **Four
anchors, four gaps**, and `A-143` is opened for the load-bearing one.

> ### ⭐⭐ UPDATED 2026-08-15 — TIER 1 IS NOW ONE SCALAR SHORT OF COMPUTABLE
>
> Two owner rulings have since closed most of anchor 1. `D-061` fixed the **set**
> (`5 · 13 · 50 · 200 · 800`), and ***"5/13 cross entry"*** fixed the **pair** — the trigger is
> **EMA(5) crossing EMA(13)**. ⛔ **The TIMEFRAME is now the only thing standing between `D-058`
> tier 1 and a computable trigger**, and it is contested: the owner's own templates split **3–2, H1
> vs M15**. ⭐ **This is the closest any entry rule in the project has come** — every other candidate
> is missing something structural; **this one is missing a single number.** §4.1A–§4.1C.
> ⚠️ **Anchors 2, 3 and 4 are untouched and still block tiers 2 and 3.**

⚠️ **And it does not agree with the course's own most mechanical entry.** V20 states
`entry = high − (candle range ÷ 3)` (`A-139`). **The owner enters at a candle CLOSE, not at a
one-third retracement.** This draft records the divergence and **does not reconcile it** — they are
different categories of claim (`D-052` §3.5), not a contradiction to be adjudicated.

---

## 1. THE ATTESTATION, RECORDED VERBATIM AND UNINTERPRETED

> **Owner, 2026-08-15:** *"the entry is the close of the candle after the ema cross. But when your
> confidence and probably [probability] is on your side then you can take it on the second leg
> hammer, railroad tracks, doji, etc. then high level is placing a limit order on the close of the
> outside bar. That's the safest entry."*

**Parsed as three tiers, and the tiering is the owner's own — *"then high level"*, *"the safest"*:**

| Tier | Trigger | Order type | Owner's own label |
|---|---|---|---|
| **1 — default** | the **close of the candle AFTER the EMA cross** | (not stated — market on close, by implication) | *"the entry"* |
| **2 — confidence-conditional** | the **second-leg** candle signal: **hammer, railroad tracks, doji, etc.** | (not stated) | *"when your confidence and probability is on your side"* |
| **3 — "high level"** | the **close of the outside bar** | ⭐ **LIMIT order** — the only order type he names | *"high level… That's the safest entry"* |

⭐ **Tier 3 is the only tier with an order type, and it is the one he calls safest.** A limit at a
level already traded is a **retracement entry** — it does not fill unless price comes back. **That
is what makes it "safest": it trades fill probability for price.** ⚠️ *That is the reading this
draft finds most natural; the owner does not say it, and §5 Part 4 does not adopt it.*

### 1.1 ⚠️ What "etc." does, and why it is not tidied away

*"hammer, railroad tracks, doji, **etc.**"* is an **open list**. ⛔ **An open list is not a
specification.** The three named are single-bar reversal signals; *"etc."* admits an unbounded
family the owner has not enumerated. **`D-030` binds**: a session may not complete the list, and
tier 2 is `DO NOT CODE` on its trigger set alone, independently of everything else wrong with it.

---

## 2. THE TIER — `OWNER EMPIRICAL PREFERENCE`, IDENTICAL TO `D-055`

The classification is settled by `D-055` §2 and is re-applied, not re-derived:

| Candidate | Verdict |
|---|---|
| `TIER 1` / `RESOLVED BY COURSE` | ⛔ **No.** The owner is not the corpus |
| `TOOLING` (`D-045`) | ⛔ **No.** That rung is for **platform artifacts**. A spoken rule is not an artifact |
| `RESOLVED — OWNER ATTESTATION` (`D-039`/`D-043`) | ⛔ **No.** That is the owner testifying to **what the course TAUGHT**. He described **what he does** |
| ⭐ **`OWNER EMPIRICAL PREFERENCE`** (`D-052`) | ✅ **Yes** — *"the owner judging what the INSTRUMENT DOES"*, *"recollection of practice, NOT course evidence"*, **outside the tiers and outside the `D-048` ladder** |

> ### THE PROPOSED STATUS, IN ONE LINE
>
> **`OWNER-ATTESTED TRADING RULE — OWNER EMPIRICAL PREFERENCE`. The owner's own practice.
> NOT COURSE-VERIFIED. It closes no `A-xxx` and no `C-xxx`.**

⭐ **No fifth tier is created.** `SOURCING_HIERARCHY.md` §3.5: *"it is **not** a fifth rung and
**not** a fifth tier — do not add one."* Followed here, as in `D-055`.

⚠️ **One thing is weaker here than in `D-055`, and it is worth naming.** `D-055`'s numbers
(25/30/50) could at least be *checked* against corpus figures. **This rule's tiers 1 and 3 name
objects the corpus never quantifies at all**, so there is nothing to check them against — the
agreement/divergence analysis in §3 is thinner by necessity, not by omission.

---

## 3. HOW IT SITS AGAINST THE LEDGERS

### 3.1 ⚠️⚠️ `A-139` — the course's most mechanical entry, and IT DISAGREES

`A-139` (V20, `OPEN`) is the closest course-side record and the comparison is the most important
thing in this section:

| | Trigger | Price |
|---|---|---|
| **V20 (`A-139`)** | *"**this** candle"* — bar unstated | ⭐ `entry = high − (candle range ÷ 3)`, worked at 100 → 33 pips |
| ⭐ **Owner (`D-058` tier 1)** | the candle **after the EMA cross** | ⭐ **the CLOSE** |

⭐⭐ **These are different entries.** A one-third retracement into a candle's range is not that
candle's close, and no arithmetic makes them the same. ⚠️ **This is NOT filed as a contradiction**
and no `C-xxx` is opened, for the reason `D-052` §3.5 fixes: **`A-139` is a record about what V20
taught; this is the owner's present practice.** They are different categories of claim, and a
category difference is not a conflict.

> ⭐ **AND THE TEMPTING MOVE IS REFUSED EXPLICITLY.** `A-139`'s single open question is **WHICH
> CANDLE** — and the owner's rule supplies a candle-selection principle (*the one after the EMA
> cross*). ⛔ **It must not be imported.** `A-139` asks which candle **V20's instructor** meant; the
> owner's selection rule is evidence about the owner. Using it would be `SOURCING_HIERARCHY.md`
> §3.2's *"do not blend"* and would hand a `TOOLING`-adjacent warrant the authority of Tier 1.
> **`A-139` stays `OPEN`. `PT-049`'s declared convention stays a convention.**

⚠️ **`A-136` is CLOSED and is not a live cross-reference.** It closed 2026-08-15 as an **ASR
artifact** (V20 R1 `M2`, item 333 — *"there is no such word"*), superseded by `A-139`. It is named
here only so a future reader does not go looking for a live record behind it.

### 3.2 ⭐ Where it TOUCHES the corpus without closing anything

| Record | Contact |
|---|---|
| **`A-011`** — M/W anatomy | Tier 2's *"**second leg**"* is the same object V19 finally gave a printed rule for (*"The Second Leg Rise can be slightly above the first, but must close below within 30M"*). ⭐ **The owner's tier 2 sits on the corpus's one M/W construction** — and `A-011` still never says how the FIRST leg is identified |
| **`A-133`** — the `blue tracer` | V19's own entry rule is *"closed below the blue tracer"*. ⭐ **Both the course's V19 entry and the owner's tier 1 trigger on a CLOSE relative to a LINE** — the same *shape*. ⛔ But V19's line is undefined and the owner's EMAs are unnamed, so **two unknowns do not make a known** |
| **`A-017`** — *"big entry candle"* | Another named entry bar with no construction. Tier 2's open list is the same failure mode |
| **`A-115`** — two entry DISQUALIFIERS with no thresholds | ⭐ The owner states an entry **qualifier** (*"confidence and probability"*) with no threshold either. **The corpus and the owner have the same gap from opposite ends** |
| **`A-090`** — board-drill offset `10` vs `5`–`8` pips | A third, different entry-offset scheme. **The corpus now holds at least three incompatible entry geometries** (`A-090`, `A-139`, and V19's close-below-the-tracer) and the owner's is a fourth |
| **`A-056`** — `Hi-Lo` | The *"limit order"* of tier 3 is the first order-type statement in the project since `A-056`'s untaught Hi-Lo entry. See `A-141_CORRECTION_PROPOSAL` |
| ⭐ **`D-055`** | ⭐⭐ **Same tier, same source, same session-family. Together: ENTRY + STOP + TP1 + TP2.** §4.2 |

### 3.3 ⛔ THE NEGATIVE RESULT, STATED PLAINLY

**No lesson in V01–V21 states an entry at "the close of the candle after an EMA cross."** A sweep of
`10_AMBIGUITIES/`, `11_CONTRADICTIONS/` and `03_LESSON_NOTES/` for EMA-cross entry triggers returns
**nothing**. The corpus's EMA material is about **which averages exist** (`A-020`, `C-010`,
`D-043`) and **confluence** (*"an M formation off the Mayo"*, V08 `[00:22:13]`), **never a cross as
a trigger.**

⭐ **This is the `D-055` §3.4 finding repeating — and this time it is the WHOLE rule, not one
number.** It is the strongest single reason §2 refuses `RESOLVED — OWNER ATTESTATION`: **an owner
recollecting the teaching would not produce a trigger the teaching never mentions.**

---

## 4. ⚠️⚠️ WHAT BLOCKS CODING — FOUR UNDEFINED ANCHORS

### 4.1 The load-bearing one — ⭐ PROPOSED `A-143` · ⭐⭐ **NARROWED TO A SINGLE ROW, 2026-08-15**

> ~~**Tier 1 says *"the EMA cross"* and does not say WHICH TWO EMAs CROSS.**~~
>
> ✅ **ANSWERED 2026-08-15 — *"5/13 cross entry"* (owner).** The trigger is **EMA(5) crossing
> EMA(13)**, and with `D-061` fixing the set the pair space collapses from ten to **one**.
> ⛔ **What remains is the TIMEFRAME**, and it is now the **only** thing standing between tier 1 and
> a computable trigger. See §4.1A.

This is not a small gap, and the project's own records make it worse rather than better:

| | |
|---|---|
| **The candidate set is disputed** | `D-043` (owner attestation, what was TAUGHT): **5 · 13 · 50 · 200 · 800**. `MMM.tpl` (owner practice, 2023): **4 · 10 · 50 · 200 · 800 · 3200**. ⚠️ **The two short periods — the ones a "cross" trigger would most plausibly use — are exactly the ones that disagree** |
| **The pair is unstated** | With six averages there are **15 possible pairs**, and the owner names none |
| **The timeframe is unstated** | *"the candle after"* presupposes a bar size. `MMM.tpl` is saved on **H1**; V19/V20's entry material is on **M15** |
| **Direction is unstated** | A cross has two directions; the rule does not say the trigger is the cross **in the trade's direction** (though that is the obvious reading, and `D-030` bars adopting it) |

```text
A-143 (proposed) -- "the EMA cross": WHICH TWO MOVING AVERAGES CROSS, on WHICH
  TIMEFRAME, in WHICH DIRECTION.
  NARROWED TWICE ON 2026-08-15 BY OWNER RULING, and it is now down to ONE ROW:
    [SET]        SETTLED. Canonical is 5/13/50/200/800 (D-061); MMM.tpl's
                 4/10/.../3200 is NOT canonical.
    [PAIR]       SETTLED. "5/13 cross entry" -- owner, 2026-08-15. The trigger
                 is EMA(5) crossing EMA(13). Ten candidate pairs -> ONE.
    [DIRECTION]  NOT STATED, but CONSTRAINED: trade direction comes from the
                 M/W (D-055 -- M at tops = short, W at bottoms = long), so the
                 cross is a TRIGGER inside an already-directional setup. That
                 the cross must agree with the M/W is the obvious reading and
                 D-030 still bars adopting it unasked.
    [TIMEFRAME]  *** OPEN. THE LAST BLOCKER. *** Never stated by the owner, and
                 HIS OWN TEMPLATES SPLIT 3-2: H1 (MMM.tpl, MMM INDICES, RS5P)
                 vs M15 (BASIC BTMM, Ultimate Blue). Tier 1's only timeframe-
                 stamped EMA fact is V09 [00:41:43] "the blueberry is the 800
                 ON THE 15 MINUTE". A 5/13 cross on M15 and on H1 are DIFFERENT
                 SIGNALS, so this is not a detail.
  DO NOT CODE -- on the timeframe alone. One owner sentence closes this record.
  Raised by: owner attestation, 2026-08-15. Basis: OWNER EMPIRICAL PREFERENCE.
  Related: A-020, C-010, D-043, D-055's anchor gap (A-142), A-139.
```

⚠️ **Numbering note:** integration holds `A-139`; the unmerged `origin/video/v21` holds **`A-140`
and `A-141`**. This record therefore takes **`A-143`**, skipping the V21 block so it survives that
merge, and leaving **`A-142`** to `D-055`'s anchor, which was raised first. ⚠️ **`D-055`'s *"perceived high"* record must renumber for the same reason** — see §6.

### 4.1A ⭐⭐ IS TIER 1 COMPUTABLE NOW? — **NO, AND THE TIMEFRAME IS THE ONLY REASON**

> *"**5/13 cross entry**"* — owner, 2026-08-15

**Tier 1 is `entry = the close of the candle after the EMA cross`. Component by component:**

| Component | State |
|---|---|
| **Which averages** | ✅ **EMA(5) and EMA(13)** — settled by this ruling |
| **Which set they come from** | ✅ **`5 · 13 · 50 · 200 · 800`** — `D-061` |
| **What the trigger event is** | ✅ **a cross of the two** |
| **What the entry price is** | ✅ **the CLOSE of the bar AFTER the cross bar** — `D-058` §1 |
| **Direction** | ⚠️ **not stated, but constrained** — see below |
| ⛔ **TIMEFRAME** | ⛔⛔ **OPEN. THE LAST BLOCKER.** |

> ### ⛔⛔ SO: NOT COMPUTABLE — BY EXACTLY ONE MISSING INPUT
>
> **Everything about the trigger is now fixed except the chart it is computed on.** ⚠️ **That is not
> a detail: a 5/13 cross on M15 and a 5/13 cross on H1 are different signals, fire at different
> times, and produce different trades.** A tool cannot be written that is agnostic to it.
>
> ⭐ **But it is a genuine milestone and the draft says so: this is the closest any entry rule in the
> project has come to computable.** Every other candidate — `A-139`'s one-third entry (bar
> unstated), V19's close-below-the-blue-tracer (`A-133`, line undefined), `A-090`'s 10-or-5-to-8 —
> is missing something *structural*. **This one is missing a single scalar.**

### 4.1B ⚠️ THE TIMEFRAME IS GENUINELY CONTESTED — the owner's own artifacts split 3–2

| Artifact | Saved timeframe |
|---|---|
| `MMM.tpl` · `MMM INDICES.tpl` · `RS5P.tpl` | ⚠️ **H1** (`period=60`) |
| ⭐ `BASIC BTMM.tpl` (the newest, 2026-08-15) · `Ultimate Blue.tpl` | ⭐ **M15** (`period=15`) |

**And Tier 1's only timeframe-stamped EMA fact points to M15:** V09 `[00:41:43]` —
*"the blueberry is the 800 **on the 15 minute time frame**"* (`RESOLVED BY COURSE`). V19/V20's entry
material is also M15.

> ⭐ **M15 is the better-supported arm and it is NOT adopted.** Three of the owner's five saved
> templates say H1 — including all three `MMM*` ones, the family named for this method — and
> `D-030` bars picking the convenient arm. ⚠️ **`!SM_Crossover_Arrows`' own alert string is
> *"Cross Up on … min … EMA"***, i.e. the tool **prints the timeframe** because it varies.
> **This is one owner sentence, and §7 Q1 asks it.**

### 4.1C ⚠️ DIRECTION — not stated, but no longer floating free

The ruling says *"cross **entry**"* and names no direction. ⭐ **It is now constrained by the
surrounding rules rather than open in the way it was:** `D-055`'s anchor ruling makes the **M/W
formation** the setup — **M at tops ⇒ short, W at bottoms ⇒ long** — so **the trade already has a
direction before the cross is consulted**, and the cross functions as a **trigger inside a
directional setup**, not as the direction-chooser.

⛔ **That the cross must AGREE with the M/W direction is the obvious reading and is NOT adopted.**
`D-030`: the owner has not said it, and a rule that fires on either cross direction inside an M is
a different rule from one that requires a down-cross. ⚠️ **Lower risk than the timeframe** — it
changes signal *count*, not signal *timing* — **but still unstated.**

---

### 4.2 The other three

| # | Anchor | Why it blocks |
|---|---|---|
| 2 | *"confidence and probability is on your side"* | ⛔ **A subjective gate with no threshold and no observable.** It is the *condition* on tier 2, so tier 2 cannot be evaluated at all. **This is the least codable sentence in either owner rule** |
| 3 | *"second leg"* | Chains to **`A-011`**. V19 gives a printed second-leg rule; **no source says how the FIRST leg is identified** |
| 4 | *"the outside bar"* | ⚠️ Standard TA has an *outside bar* (higher high **and** lower low than prior). ⛔ **The course never defines it** and the owner does not either. **Importing the retail definition is `D-030`/`A-082`** |

⚠️ **And a fifth, smaller, genuinely ambiguous point on tier 3:** *"a limit order **on the close** of
the outside bar"* — a limit **at the close price**, or a limit **placed at the moment the bar
closes** (at some other level)? **These are different orders.** §7 Q4.

> ### ⭐ THE SHAPE OF THIS RECORD, STATED SO IT IS NOT MISREAD
>
> **`D-058` supplies a THREE-TIER LADDER and no executable trigger — exactly as `D-055` supplied
> three numbers and no geometry.** Both are valuable, both are the owner's real practice, and
> **neither is codable.** ⭐ **The pattern is now twice-confirmed and worth naming: the owner's rules
> are stated in the corpus's own vocabulary, and that vocabulary is what the corpus never defined.**

---

## 5. THE PROPOSED LEDGER ENTRY — TEXT FOR THE OWNER TO APPROVE, EDIT OR REJECT

> ## D-058 — The owner's three-tier ENTRY rule is recorded as an OWNER-ATTESTED TRADING RULE at OWNER EMPIRICAL PREFERENCE; it is NOT COURSE-VERIFIED; it closes NO record; and all four of its anchors are undefined
>
> **Date:** 2026-08-15
> **Category:** `OWNER EMPIRICAL PREFERENCE` (`D-052`) — the owner's judgment from lived practice,
> **outside the tiers and outside the `D-048` ladder**. Creates **no new tier and no new rung**
> (`SOURCING_HIERARCHY.md` §3.5). **Companion to `D-055`**, same tier, same source.
>
> **Part 1 — the rule, as stated.** The owner attests, first person, 2026-08-15, a **three-tier
> entry ladder**: **(1)** default — entry at **the close of the candle after the EMA cross**;
> **(2)** when *"confidence and probability"* favour it — entry on the **second-leg** candle signal
> (**hammer, railroad tracks, doji, etc.**); **(3)** *"high level"* / *"the safest entry"* — a
> **LIMIT order at the close of the outside bar.** ⭐ With `D-055` this is **the project's first
> complete trade template from one source: entry, stop, TP1, TP2.**
>
> **Part 2 — what it is NOT.** ⛔ **NOT `RESOLVED BY COURSE`. NOT `RESOLVED — OWNER ATTESTATION`.
> NOT `TOOLING`.** ⭐ **No lesson in V01–V21 states an EMA-cross entry trigger at all** — the
> corpus's EMA material concerns which averages exist and confluence, never a cross as a trigger.
> An owner recollecting the teaching would not produce a trigger the teaching never mentions. Any
> artifact citing this rule cites it as **the owner's**, never as the course's.
>
> **Part 3 — ⛔ NOT EXECUTABLE. Four undefined anchors, and `A-143` is opened for the first.**
> **(1)** ⭐⭐ ***"the EMA cross"* — NARROWED TWICE ON 2026-08-15 AND NOW DOWN TO ONE ROW.**
> ✅ The **set** is settled (`D-061`: `5 · 13 · 50 · 200 · 800`). ✅ The **pair** is settled — the
> owner ruled ***"5/13 cross entry"***, so the trigger is **EMA(5) crossing EMA(13)**; ten candidate
> pairs → **one**. ⚠️ **Direction** is unstated but **constrained** — `D-055`'s M/W anchor already
> fixes trade direction, so the cross is a trigger *inside* a directional setup.
> ⛔⛔ **TIMEFRAME IS THE LAST BLOCKER and it is genuinely contested: the owner's own templates split
> 3–2, H1 (`MMM.tpl`, `MMM INDICES`, `RS5P`) against M15 (`BASIC BTMM`, `Ultimate Blue`)**, while
> Tier 1's only timeframe-stamped EMA fact — V09 `[00:41:43]`, *"the 800 **on the 15 minute**"* — is
> M15. **A 5/13 cross on M15 and on H1 are different signals.** → **`A-143`, `DO NOT CODE`, on the
> timeframe alone.**
> **(2)** *"confidence and probability is on your side"* — a **subjective gate with no threshold**;
> it is the *condition* on tier 2, so tier 2 cannot be evaluated. **(3)** *"second leg"* — chains to
> **`A-011`**, which still never says how the FIRST leg is identified. **(4)** *"the outside bar"* —
> **undefined by the course and by the owner**; importing the retail definition is the `A-082` error.
> ⚠️ Plus an ambiguity inside tier 3: a limit **at the close price**, or **placed when the bar
> closes**? Different orders. **`D-030` binds on all of it.**
>
> **Part 4 — the divergence from `A-139`, recorded and NOT reconciled.** V20 states
> `entry = high − (candle range ÷ 3)`; the owner enters at a **close**. ⭐ **These are different
> entries and no arithmetic makes them the same.** ⚠️ **No `C-xxx` is opened**: `A-139` is a record
> about **what V20 taught**, this is **the owner's practice**, and a category difference is not a
> conflict (`D-052` §3.5). ⛔⛔ **And `A-139`'s open question — WHICH CANDLE — must NOT be answered
> from this entry.** The owner's selection principle is evidence about the owner;
> importing it is `SOURCING_HIERARCHY.md` §3.2's *"do not blend"*. **`A-139` stays `OPEN` and
> `PT-049`'s declared convention stays a convention.**
>
> **Part 5 — what does NOT close.** ⛔ **`A-139`, `A-133`, `A-017`, `A-115`, `A-090`, `A-056`,
> `A-011`, `A-020` all untouched.** ⚠️ `A-136` is **CLOSED** (ASR artifact, V20 R1 `M2`) and is
> referenced only so no reader hunts for a live record behind it. ⭐ Note for the corpus's benefit:
> with this rule the project now holds **four incompatible entry geometries** — `A-090`'s 10 or 5–8
> pips inside, `A-139`'s one-third retracement, V19's close-below-the-blue-tracer, and this — and
> **that count is itself a finding about the corpus.**
>
> **Reason:** twenty-one lessons have produced no complete, self-consistent entry rule; `A-139` gives
> arithmetic without a bar, V19 gives a bar without a definable line, `A-090` gives an offset that
> contradicts itself. The owner's own practice is the only complete ladder in existence, and
> recording it accurately at a weak tier is strictly better than coding it as doctrine or losing it.
>
> **Evidence:** owner attestation, 2026-08-15 (§1, verbatim). `A-139` (V20 `[00:29:14]`–`[00:29:53]`);
> `A-136` (CLOSED); `A-133`; `A-011` + V19's printed second-leg slides; `A-017`; `A-115`; `A-090`;
> `A-056`; `A-020`, `C-010`, `D-043`; `MMM.tpl` (md5 `db617bcdfeb5df26c033036f96c41472`, six EMAs);
> `D-030`, `D-045`, `D-052`, `D-055`; `SOURCING_HIERARCHY.md` §3.2, §3.4, §3.5.
>
> **Alternatives considered:** *Using the owner's candle-selection rule to close `A-139`* —
> **rejected; this is the central discipline of the entry.** *Recording it as
> `RESOLVED — OWNER ATTESTATION`* — rejected; the corpus never mentions an EMA-cross trigger, so
> this is practice, not recollection of teaching. *Admitting it to `TOOLING`* — rejected; a spoken
> rule is not a platform artifact. *Filing a `C-xxx` against `A-139`* — rejected; different
> categories of claim, not a contradiction. *Adopting the retail definition of "outside bar"* —
> rejected; `D-030`/`A-082`. *Completing the "etc." list of candle signals* — rejected; `D-030`.
> *Reading tier 3's limit as a retracement entry* — **not adopted**, though it is the natural
> reading and is why he may call it safest; the owner does not say it.
>
> **Consequences:**
>
> 1. ⭐ **`A-143` is opened** (*"the EMA cross"*), `DO NOT CODE`. **One owner sentence clears it**,
>    as with `D-055`'s anchor.
> 2. ⛔ **No `PT`/`BT` may use tiers 1–3 while `A-143` is open** — there is no trigger to compute.
>    Any later run states in its pre-registration that the values are **`OWNER EMPIRICAL
>    PREFERENCE`, not course-derived** (`D-027`).
> 3. ⚠️ **The §3.4 re-check obligation attaches.** `D-052` established that an owner preference is
>    **overturnable by a later Tier 1 statement**. **Any lesson stating an entry trigger, an entry
>    bar, or an order type triggers `SOURCING_HIERARCHY.md` §3.1 against this entry.** ⭐ **V21 is
>    ingested but UNMERGED (`origin/video/v21`) and is the live risk** — it is the money-management
>    lesson and it demonstrably discusses **order placement** (`A-141`), which is tier 3's subject.
> 4. `12_MASTER_SPEC` / `13_MACHINE_SPEC` gain **no** entry parameters from this entry.
> 5. ⭐ Read with `D-055`, the project now holds a **complete owner trade template** — entry, stop,
>    two targets — of which **not one component is executable.** That is recorded as a *state of the
>    project*, not as a complaint: it localises exactly what is missing.
>
> **Status:** ACTIVE — `OWNER EMPIRICAL PREFERENCE`, **NOT COURSE-VERIFIED**, **NOT EXECUTABLE**
> while `A-143` is open

---

## 6. ⚠️ A COLLISION THIS DRAFT INHERITED AND MUST NOT REPEAT

`D-055` renumbered its *"perceived high"* ambiguity to **`A-136`** and `D-056` proposed **`C-030`**.
⛔ **Both numbers were then taken by the V20 session on integration** — `A-136` (closed as an ASR
artifact) and `C-030` (V20's time-cap contradiction). **Both must renumber**, and the V21 block
(`A-140`, `A-141`, `C-031` on the unmerged `origin/video/v21`) must be skipped too:

| Record | Was | ⭐ **Now** |
|---|---|---|
| `D-055`'s *"perceived high"* anchor | `A-132` → `A-136` | ⭐ **`A-142`** |
| `D-056`'s ribbon-vs-slide contradiction | `C-029` → `C-030` | ⭐ **`C-032`** |
| This draft's *"EMA cross"* anchor | — | ⭐ **`A-143`** |

⭐ **Two distinct anchor records, two distinct numbers.** `D-055`'s *"perceived high"* was raised
first and takes **`A-142`**; this draft's *"the EMA cross"* takes **`A-143`**. The `D-055` and
`D-056` renumberings are applied in the same commit as this draft, so no file is left pointing at a
number V20 already holds.

---

## 7. THE QUESTIONS FOR THE OWNER

1. ⭐⭐ **WHICH TIMEFRAME?** §4.1B. ✅ *Which two EMAs* is **ANSWERED** — *"5/13 cross entry"* — so
   this is **the single remaining input** between tier 1 and a computable trigger. Your own templates
   split **3–2 (H1 vs M15)**; V09's Tier 1 *"800 on the 15 minute"* points M15. **One word settles
   it.**
   ~~**Which two EMAs, and on which timeframe?** §4.1.~~ **The single highest-value sentence you
   could supply** — it is the whole of tier 1 and it closes `A-143`. *(And if the pair involves the
   fast averages, please also settle whether they are your chart's **4 and 10** or the course's
   **5 and 13** — `D-043` and `MMM.tpl` disagree.)*
2. **What makes confidence/probability "on your side"?** §4.2. Even a rough checklist converts tier
   2 from unevaluable to testable.
3. **What is an "outside bar" to you?** — higher high *and* lower low than the prior bar, or
   something else? §4.2.
4. **Tier 3: is the limit AT the close price of the outside bar, or placed at that moment at some
   other level?** §4.2 — these are different orders and every backtest of tier 3 depends on it.
5. **Are the three tiers alternatives or a sequence?** — do you take whichever presents, or wait for
   the safest and drop to tier 1 only if it never comes?
6. **Does the stop/target rule of `D-055` apply identically to all three tiers?** A limit entry at a
   retracement has a different distance to the M/W extreme than a close entry does — so **25 pips
   beyond the perceived high is a different risk on tier 3 than on tier 1.**
